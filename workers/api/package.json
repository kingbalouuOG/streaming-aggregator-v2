{
  "name": "videx-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir=.wrangler-check"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
