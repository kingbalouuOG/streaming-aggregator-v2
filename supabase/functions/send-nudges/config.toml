# Per-function config for send-nudges.
# Cron-invoked every 15 minutes with the Vault-backed service-role JWT
# (migration 095, the 059 pattern). verify_jwt stays true; the handler
# additionally asserts role === 'service_role'. Growth G2 H4.

verify_jwt = true
