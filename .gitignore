# Dependencies
node_modules/

# Build output
dist/

# Environment & secrets
.env
.env.local
.env.*.local
api-keys.txt

# Vite
*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
Desktop.ini

# TypeScript
*.tsbuildinfo

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Capacitor Android build artifacts
android/app/build/
android/app/release/
android/.gradle/
android/build/
android/captures/
android/.idea/
*.apk
*.aab
*.jks
*.keystore

# Debug logs
debug.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Windows artifacts
nul

# V1 archive (local reference only)
_archive/

# Test results & artifacts (local reference only)
test-results/

# Claude Code workspace (user-specific)
.claude/

# Playwright
playwright-report/
playwright/.cache/
.playwright-mcp/
tmp/

# Dev screenshots — Playwright walkthroughs etc. drop PNGs at the
# repo root; keep them out of git. Subdirectory PNGs (assets, public)
# are unaffected.
/*.png
/*.jpg
/*.jpeg

# Native device-test scratch (frame-forensics screenshots, build logs)
.audit/

# VS Code Edge Tools per-workspace profile (auth state, history, etc.)
.vscode/edge-profile/

# Temporary / scratch files
temp_*.json
quiz_logs.json
temp_logs.json

# One-off analysis scripts & test data
scripts/quiz-report-*.js
scripts/*-results.json
scripts/audit-results/*.json
scripts/*-output.txt
scripts/*-progress*.txt
p2-*.csv

# Phase 0.5 enrichment backfill state (local only)
scripts/enrichment/.checkpoint.json
scripts/enrichment/.checkpoint.json.tmp
scripts/enrichment/.failures.jsonl

# Phase 1 embedding backfill state (local only)
scripts/embeddings/.checkpoint.json
scripts/embeddings/.checkpoint.json.tmp
scripts/embeddings/.failures.jsonl

# Phase 2 fingerprint build state (local only)
scripts/fingerprints/.checkpoint.json
scripts/fingerprints/.checkpoint.json.tmp
scripts/fingerprints/.failures.jsonl

# Phase 2.5 TMDb backfill state (local only)
scripts/fingerprints/.backfill-checkpoint.json
scripts/fingerprints/.backfill-failures.jsonl
.mcp.json

# PLAT-2 worker build artifacts
workers/api/.wrangler-check/
workers/api/.wrangler/
_frames/
_fwin*/
*.mp4
