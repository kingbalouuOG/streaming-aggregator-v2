# Handoff: Growth G2 session H4, household nudges (migration 095, send-nudges, policy wording)

**Date:** 2026-09-18 · **Source:** [G2 plan](2026-09-17-004-feat-phase-g2-household-loop-plan.md) §1 item 4, §2 item 2 (the notifications audit), §3, §4 H4, §5 (095), §9 D9/D10/D13/D15, §11b to §11d · **Size:** L, SQL plus two Edge Functions plus app plus Worker plus policy · **After:** H2 (PR #217) and H3 (PR #216) merged; IN-GR-034 merged (tokens now exist). **Folded in:** IN-GR-041 (push-open events need a JWT and an ownership check) and IN-GR-026 (a per-push id). **Followed by:** H5 verification, then the 2.6.0 release.

**Joe, before starting:** nothing. **During the session:** apply migration 095 in Studio when handed over (never `db push`); it registers the 15-minute cron, so the Vault entry `service_role_key` must still exist (059 checks). Deploy both functions after review: `npx supabase functions deploy send-notifications` and `npx supabase functions deploy send-nudges` (keep `verify_jwt = true` on both). **Approve the policy wording (D15) before the PR merges**; the store privacy forms are re-checked afterwards.

---

```
Context: Videx. Supabase (migrations applied by Joe in Studio; pg_cron calls Edge Functions through net.http_post with the Vault service_role_key, see migration 059; pg_net cuts a call at 30 seconds, so a function must answer fast; cron.job_run_details "succeeded" only means the request was queued, the truth is net._http_response), Edge Functions in supabase/functions (Deno; deploys are Joe's), live RN/Expo 56 app in native/ (native/src/lib is a junction to src/lib; never recursive-delete inside native/), Cloudflare Worker in workers/api. Read first, in this order: docs/plans/2026-09-17-004-feat-phase-g2-household-loop-plan.md §1, §2 item 2, §3, §4 H4, §5 row 095, §9 D9/D10/D13/D15, §11b to §11d; supabase/functions/send-notifications/index.ts (NOTIFICATION_TYPES, getEnabledTypes, hitDailyCap with its 20-hour window, the claim-then-send step, the Expo send and receipt code at the bottom that you extract) and compose.ts (composeMessage and its tests); supabase/migrations/056_notification_preferences.sql, 057_notification_deliveries.sql, 059_notifications_cron.sql (the cron pattern you copy), 093_households.sql and 094_household_remove_member.sql (the tables you read); native/src/providers/notifications.tsx (pushOriginType collapses unknown types to 'bundle'; routeFromData string-strips videx:// and pushes; notification_opened emission), native/src/components/profile/ProfileNotifications.tsx (ROWS), native/src/app/list/[id].tsx (the push landing; read only), src/lib/growth/inboundLink.ts (parseInboundLink routes /list/{id} since H3), workers/api/src/index.ts and growthEvents.ts (the POST growth route; how the JWT is verified today), docs/legal/privacy-policy.md ("Push notifications" section) and native/src/legal/policyContent.ts (its mirror), supabase/queries/growth-dashboard.sql (§6c and §6d hardcode the two types), videx-wiki/wiki/concepts/architecture/notifications-v1.md, wiki/entities/codebase/event-taxonomy.md, wiki/registers/parking-lot.md (IN-GR-026, IN-GR-041), videx-wiki/wiki/concepts/product/tone-and-voice-guide.md (no em dashes, no exclamation marks), docs/CONVENTIONS.md, videx-wiki/AGENTS.md. Worktree branch off main named feat/growth-h4-nudges, relative paths. Migration number 095 is yours and the only one this session writes.

Goal: when a member adds titles to the shared list, the other members get one bundled push within the hour, capped independently of arrival and leaving-soon alerts, quiet at night, opt-out in Profile, and the tap lands on the list. Everything about it is measured, the policy says it, and push-open events can no longer be forged.

Decided (do not reopen): a separate send-nudges function on a 15-minute cron (D9); per-type cap groups (titles: arrival and leaving_soon; household: household_nudge) with a global floor of 3 pushes per person per rolling 24 hours across groups, and quiet hours 22:00 to 08:00 Europe/London for nudges (D9); daily digest is a stub, no code (D10); nudge opens are notification_opened growth events with metadata.kind (D13); v1 nudges on adds only (a "tonight" reaction nudge is filed, not built).

Do, in this order:

1. Pure policy module first, supabase/functions/_shared/pushPolicy.ts (Web APIs only, no Deno, testable by the root vitest rig: add supabase/functions/_shared/__tests__ if the include list needs it and supabase/functions/send-nudges/__tests__ to vitest.config.*): capGroupFor(type), the group cap rule (one push per group per 20 hours, today's window), the global floor (3 per 24 hours across groups), isQuietHour(now, 'Europe/London') using Intl with an explicit time zone, and nudgeWindowStart(now) = the current hour truncated in UTC. Tests with fixed clocks either side of both boundaries and across the BST change.

2. Migration supabase/migrations/095_household_nudges.sql, additive: (a) notification_preferences.notification_type CHECK gains 'household_nudge' (absent row still means enabled, per 056); (b) notification_deliveries: CHECK gains 'household_nudge'; tmdb_id and media_type become nullable; add list_id uuid references watchlists on delete set null, nudge_window timestamptz, push_id uuid; a CHECK that title types carry tmdb_id and media_type and household_nudge carries list_id and nudge_window; drop uq_notification_deliveries_dedup and recreate it as a partial unique index WHERE notification_type IN ('arrival','leaving_soon') with the same columns, plus a second partial unique index (user_id, notification_type, list_id, nudge_window) WHERE notification_type = 'household_nudge'; an index on (user_id, sent_at DESC) if none exists (the cap query); (c) the cron: copy 059's Vault check and net.http_post shape for a job named 'send-nudges-15m' on '*/15 * * * *' calling /functions/v1/send-nudges, idempotent unschedule first; (d) no re-emit of delete_own_account or export_user_data (092 already deletes notification_deliveries by user; say so in the header). Header comments in the 093 style with the reversibility block. Verify script supabase/queries/verify-095-nudges.sql in the 093 style (rolls back): both partial indexes enforce, the CHECK refuses a nudge without list_id and a title alert without tmdb_id, the cron job row exists.

3. Extract supabase/functions/_shared/expoPush.ts from send-notifications/index.ts: the Expo send (chunking, tickets), receipt polling and dead-token pruning, behaviour-preserving; send-notifications imports it; then change hitDailyCap there to the group rule plus the global floor from pushPolicy (title alerts keep their 08:00 run and are exempt from quiet hours). Every existing compose and cap test still passes; add one for "a nudge earlier today does not block an arrival".

4. supabase/functions/send-nudges/index.ts (+ config.toml with verify_jwt = true and the service-role assertion send-notifications uses), plus a pure compose.ts: (a) at each run, skip entirely during quiet hours; (b) candidates: for each watchlist with items whose added_at is after the later of (now minus 60 minutes) and the recipient's last household_nudge sent_at for that list, group by recipient (every member with an enabled preference and a live push token, excluding the adder), and bundle: "{username} added {n} titles to {household}" / "{a} and {b} added {n} titles to {household}" / "{a} and 2 others added…"; a single title names it: "{username} added {title} to {household}"; body line: "Open Videx to react." (tone guide); (c) claim then send, as today: insert the delivery rows (type household_nudge, list_id, nudge_window from pushPolicy, one push_id uuid shared by the rows of one send, tmdb_id null) and let the partial unique index refuse a second nudge in the same hour window; apply the group cap and the global floor before claiming; (d) payload data { url: 'videx://list/{listId}', type: 'household_nudge', delivery_id, push_id, via: 'push' }, never the invite token; (e) finish well under 30 seconds: cap the batch (say 200 recipients per run) and let the next run take the rest. Tests for compose (all bundle shapes, no dashes) and for the candidate selection as a pure function over fixture rows.

5. App: native/src/providers/notifications.tsx: pushOriginType recognises 'household_nudge' (it no longer collapses to 'bundle'); routeFromData resolves the route with parseInboundLink so a list url lands on /list/{id} (H3's parser) and any unknown path still falls back as today; notification_opened for a nudge carries object { type: 'list', id }, delivery_id and metadata { type: 'household_nudge', kind: 'household_nudge', push_id }. ProfileNotifications gains a row { type: 'household_nudge', label: 'Household activity', hint: 'When someone adds to a shared list' } with the same toggle plumbing as the two existing rows (preference row upsert). The list screen needs no change beyond being the landing (newest first already); a "reply" moment on the list is filed, not built.

6. IN-GR-041 fold-in, workers/api: the POST growth-events route must require a verified JWT for notification_opened (reject 401 without one) and, when delivery_id is present, check with the service-role client that notification_deliveries.id belongs to that user (403 otherwise; a missing row is 403 too). Keep every other event's behaviour byte-identical; add tests for the three outcomes with the route's existing test pattern.

7. Measurement: supabase/queries/growth-dashboard.sql: §6c and §6d take the third type (nudge opens counted by push_id, which also closes IN-GR-026 for bundles); a new §7 "Household loop": households created per 100 sign-ups, members per household (distribution), households with two or more members active in the last 30 days (a member is active if they added or reacted), shared-list adds per household per week, nudge-to-open rate (opens with metadata.kind = 'household_nudge' over nudge deliveries). Plain SQL, no new views.

8. Policy (D15; Joe approves before merge): docs/legal/privacy-policy.md "Push notifications": the alert types gain "household activity: when another member adds titles to a shared list you belong to"; the push content sentence becomes "the title name and a short availability line, or a member's username and the shared list name"; the delivery-log sentence covers "for which title or shared list". Date the change and keep it non-material (no notice period). Mirror it in native/src/legal/policyContent.ts exactly. Say in the PR whether the App Store and Play data-safety forms change (expected: no new data category; state your reasoning).

9. Gates: root npm run lint (0 errors), npm test, npm run build; native npx tsc --noEmit and npm run lint (root npm install first); workers/api npm run check and tests; deno check on send-notifications and send-nudges. IN-GR-022 is closed; deno check must be clean.

10. Docs: videx-wiki/wiki/concepts/architecture/notifications-v1.md (three types, cap groups, quiet hours, the second function, the cron), wiki/entities/codebase/database-schema.md and migrations.md (095 applied), event-taxonomy.md (nudge opens), the Worker entity page (the JWT rule), parking-lot.md (close IN-GR-026 and IN-GR-041; file the "tonight" nudge and the list "reply" moment as new rows), log.md. Leave plan §11 to the strategy thread.

11. One PR; merge is Joe's, after the policy wording is approved and 095 is applied and verified live. Device check before merge (Joe, iPhone by OTA to the preview build; Android waits for the versionCode 18 build in H5): account A adds two titles to the shared list outside quiet hours; account B's phone gets one push within 15 minutes naming A and the household; tapping it opens the list and writes one notification_opened with object list and a push_id; a second add by A within the hour produces no second push; B's Profile → Notifications shows the Household activity toggle and turning it off stops the next one. End with a summary for the strategy thread: the cap and quiet-hour rules as implemented, the payload, what H5 must test on Android, and anything that changes the 2.6.0 release shape.

Out of scope: the digest (stub only, D10), reaction nudges, Realtime, any change to the household tables, the list screen's UI, store form submissions themselves (Joe), migrations other than 095.
```
