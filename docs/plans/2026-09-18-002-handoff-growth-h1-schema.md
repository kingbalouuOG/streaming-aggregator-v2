# Handoff: Growth G2 session H1, household schema and RPCs (migration 093)

**Date:** 2026-09-18 · **Source:** [G2 plan](2026-09-17-004-feat-phase-g2-household-loop-plan.md) §3, §4 H1, §5 (093), §6, §8, §9 (all decisions accepted 18 Sept) · **Size:** M, SQL plus typegen and a small shared-lib edit · **Runs after:** nothing; H1 is first. The IN-GR-034 and hygiene sessions run in parallel and touch no migration. **Followed by:** H2 (app) and H3 (links) in parallel once 093 is applied; their handoffs are written from this session's summary because they build against the RPC signatures fixed here.

**Joe, before starting:** nothing. **During the session** you apply 093 in the Studio SQL editor when the session says the file is ready (never `supabase db push`), then run the verification script it hands you and paste the NOTICE output back. Typegen happens after apply.

---

```
Context: Videx. Supabase project with migrations in supabase/migrations (applied by Joe in Studio, never db push; remote schema_migrations is not authoritative, verify live with to_regclass / to_regprocedure / pg_policies), shared lib in src/lib (native/src/lib is a junction to it; never recursive-delete inside native/), Cloudflare Worker in workers/api. Read first, in this order: docs/plans/2026-09-17-004-feat-phase-g2-household-loop-plan.md (§2 item 1 and item 3 for the audit facts, §3, §5 for the 093 shape, §6, §8, §9 for the decisions, all accepted as recommended), docs/CONVENTIONS.md (migration rules, typegen-check CI), supabase/migrations/092_growth_scope_and_username_check.sql (the current delete_own_account and export_user_data bodies you will re-emit; the username_available rate-limit pattern from 053), supabase/migrations/088_shared_rooms.sql (gen_random_uuid ids, profiles FKs, RLS and grants style), supabase/migrations/060_*.sql (claim_push_token: SECURITY DEFINER with possession-of-token as the right), supabase/migrations/090_growth_events.sql (the event_name CHECK), supabase/queries/verify-089-handle-new-user-placeholder.sql (the verification-script style: one transaction, ASSERTs, NOTICEs, ROLLBACK), src/lib/auth/username.ts (the client username rule the CHECK must mirror), src/lib/growth/growthEvents.ts and workers/api/src/growthEvents.ts (where event names live), videx-wiki/wiki/entities/codebase/database-schema.md, videx-wiki/wiki/entities/codebase/migrations.md, videx-wiki/wiki/entities/codebase/event-taxonomy.md, videx-wiki/wiki/registers/parking-lot.md (IN-GR-043, IN-GR-009), videx-wiki/AGENTS.md. Worktree branch off main named feat/growth-h1-schema, relative paths. Migration number 093 is yours and is the only migration this session writes; 094 is reserved for H4.

Goal: the household entity exists in the database with membership row security and the five edge RPCs, verified live, typed, and documented, so H2 (app) and H3 (links) can build against fixed signatures. No app code beyond the event-name constant. No Worker code. No nudges. The personal watchlist table is never altered.

Decisions already taken (do not reopen): additive schema, personal watchlist untouched (D1); owner plus members, cap 6 per household, a person may belong to several households (D2, D3); exactly one shared list per household in v1, the watchlists table supports more later (D3); invite is a revocable server-minted uuid token, 7-day expiry, multi-use up to the cap, owner revokes, a new invite replaces the old (D4, D5); RLS for reads and writes, SECURITY DEFINER RPCs for create, invite, join, leave and member names (D6, D7); reactions up, down, tonight, one per member per item (D11); leaving: items stay with added_by nulled, reactions cascade, sole-owner deletion deletes the household, otherwise ownership passes to the oldest member (D12); one new growth event household_joined (D13).

Do, in this order:

0. Capture before you create. With the Supabase MCP (read-only queries only) record the live personal watchlist definition: pg_get_constraintdef for its unique constraint (find its name), pg_indexes, pg_policies, and the grants. Put the result as a comment block at the top of 093 under "Live watchlist definition captured 2026-09-xx (read-only; not altered by this migration)" and also as supabase/queries/capture-watchlist-ddl.sql so it can be re-run. While there, answer the plan's §8 open facts and put the answers in your summary: (1) any policy anywhere that grants cross-user reads; (2) whether Realtime is enabled on the project; (3) counts of user_push_tokens rows and of installs in growth_events that carry more than one user_id.

1. Tables, in supabase/migrations/093_households.sql, all additive, all with RLS enabled and the anon role granted nothing:
   - households(id uuid pk default gen_random_uuid(), name text not null check (char_length(name) between 1 and 40), owner_id uuid not null references public.profiles(id) on delete cascade, created_at timestamptz not null default now()).
   - household_members(household_id uuid references households on delete cascade, user_id uuid references public.profiles(id) on delete cascade, role text not null check (role in ('owner','member')), joined_at timestamptz not null default now(), primary key (household_id, user_id)); index on user_id.
   - watchlists(id uuid pk default gen_random_uuid(), household_id uuid not null references households on delete cascade, name text not null default 'Shared' check (char_length(name) between 1 and 40), created_by uuid references public.profiles(id) on delete set null, created_at timestamptz not null default now()); index on household_id. No constraint limiting a household to one list; v1 creates exactly one through create_household and the app never creates a second.
   - watchlist_items(id uuid pk default gen_random_uuid(), watchlist_id uuid not null references watchlists on delete cascade, tmdb_id integer not null, media_type text not null check (media_type in ('movie','tv')), title text not null, poster_path text, added_by uuid references public.profiles(id) on delete set null, added_at timestamptz not null default now(), unique (watchlist_id, tmdb_id, media_type)).
   - watchlist_reactions(item_id uuid references watchlist_items on delete cascade, user_id uuid references public.profiles(id) on delete cascade, reaction text not null check (reaction in ('up','down','tonight')), created_at timestamptz not null default now(), updated_at timestamptz not null default now(), primary key (item_id, user_id)).
   - household_invites(token uuid pk default gen_random_uuid(), household_id uuid not null references households on delete cascade, created_by uuid not null references public.profiles(id) on delete cascade, created_at timestamptz not null default now(), expires_at timestamptz not null default now() + interval '7 days', max_uses integer not null default 6, uses integer not null default 0, revoked_at timestamptz); index on household_id. RLS enabled with NO policies: nobody reads or writes it except the RPCs.

2. Row security for the authenticated role, the first membership policies in the codebase. Write one helper, is_household_member(hid uuid) returns boolean, SECURITY DEFINER, STABLE, search_path pinned, that checks household_members for auth.uid(), and use it in every policy so the shape is uniform and the planner does not re-evaluate a correlated subquery per row. Policies: households SELECT for members, UPDATE (name only, enforce with a trigger or by granting UPDATE on the name column alone) for the owner, no direct INSERT or DELETE (RPCs); household_members SELECT for members of the same household, no direct writes; watchlists SELECT for members, no direct writes in v1; watchlist_items SELECT for members, INSERT for members with added_by = auth.uid() enforced in the WITH CHECK, DELETE for the adder or the household owner, no UPDATE in v1; watchlist_reactions SELECT for members, INSERT and UPDATE and DELETE for the caller's own row with user_id = auth.uid(). Grants follow 088: revoke all from public and anon, grant the specific verbs to authenticated. State the exact policy names in the summary.

3. RPCs, all SECURITY DEFINER with SET search_path = public, pg_temp, EXECUTE revoked from public and anon and granted to authenticated, every failure a RAISE EXCEPTION with a stable code in the message that the app maps to copy (use these exact codes):
   - create_household(p_name text) returns table (household_id uuid, watchlist_id uuid). Creates the household with owner_id = auth.uid(), the owner member row, and one watchlist named 'Shared' with created_by = auth.uid(). Guard: a person may own at most 3 households (code household_limit).
   - create_invite(p_household_id uuid) returns table (token uuid, expires_at timestamptz). Owner only (code not_owner). Sets revoked_at = now() on any active invite for the household first (a new invite replaces the old), then inserts one with the defaults. Rate limit: at most 10 invites per owner per rolling 24 hours (code rate_limited), counted from household_invites itself, no counter table.
   - join_household(p_token uuid) returns table (household_id uuid, watchlist_id uuid, already_member boolean). Checks in this order and raises on the first failure: token exists and revoked_at is null (invite_invalid), expires_at > now() (invite_expired), uses < max_uses (invite_exhausted), member count < 6 (household_full). If the caller is already a member, return their ids with already_member = true and do not increment uses (idempotent). Otherwise insert the member row with role 'member', increment uses, return already_member = false. The caller's identity is auth.uid() only; the token is the proof of right, as in claim_push_token. Do not reveal whether a token ever existed beyond invite_invalid.
   - leave_household(p_household_id uuid) returns void. Caller must be a member (code not_member). Delete the caller's reactions in that household's lists, set added_by = null on the items they added there, delete their member row. If they were the owner: if other members remain, set households.owner_id to the member with the earliest joined_at and that member's role to 'owner'; if none remain, delete the household (cascades lists, items, invites). Put the body in an internal function household_leave_internal(p_user_id uuid, p_household_id uuid) with EXECUTE granted to nobody, so delete_own_account can call the same logic.
   - household_members_view(p_household_id uuid) returns table (user_id uuid, username text, role text, joined_at timestamptz). Caller must be a member (code not_member). Reads profiles.username under the definer; ordered by joined_at. Usernames only, no email, no display name.

4. Re-emit delete_own_account and export_user_data from 092 wholesale (the convention: every user-scoped table re-emits both bodies; do not patch). Deletion: before the existing body, for each household the caller belongs to, call household_leave_internal(auth.uid(), household_id) so D12's semantics hold (sole-owner households vanish, others pass ownership, added_by nulls, reactions go). Export: add a households array (id, name, role, joined_at), the items the caller added (watchlist_id, tmdb_id, media_type, title, added_at) and their reactions (item_id, reaction, created_at). Keep the 092 install-id scoping exactly as it is.

5. growth_events.event_name CHECK gains 'household_joined' (ALTER TABLE ... DROP CONSTRAINT / ADD CONSTRAINT with the full list; find the constraint name live first). Then add the name wherever the client and Worker lists live (src/lib/growth/growthEvents.ts GROWTH_EVENT_NAMES and its union member with object type 'list' and metadata { household_id }; workers/api/src/growthEvents.ts if it keeps its own list; any test that asserts the two agree). Root npm test and workers/api npm run check must pass.

6. IN-GR-043 fold-in: read the rule src/lib/auth/username.ts enforces (length, charset, reserved words if any) and add a CHECK on profiles.username that mirrors the length and charset exactly (not the reserved-word list; that stays client and RPC side). First scan live rows with the MCP (count of rows that would violate, and which); if any violate, add the constraint NOT VALID and say so in the summary rather than failing the apply, and list the offending usernames for Joe to decide. Placeholder usernames from 089 (user_ plus 8 hex) must pass.

7. Verification script supabase/queries/verify-093-households.sql in the 089 style: one transaction ending in ROLLBACK, two fixture users created directly in auth.users with the .invalid TLD plus their profiles rows, then: create_household as user A; household_members_view as A shows one owner; as user B (set role authenticated, set request.jwt.claims) the household is invisible under RLS and household_members_view raises not_member; create_invite as A; join_household as B (already_member false), again as B (already_member true, uses unchanged), as a third user with an exhausted or expired or revoked token (each code); a member cap case (max_uses 6 with 6 members then household_full for a seventh, done by lowering max_uses in the fixture rather than creating seven users if that is simpler, but the cap must be exercised once); item insert as B with added_by spoofed to A is rejected by WITH CHECK; reaction upsert as B visible to A; leave_household as A passes ownership to B; delete_own_account as B on a sole-owner household deletes it; as anon every table returns zero rows and every RPC is not executable. ASSERT each, NOTICE on success. Run it yourself against the live project after Joe applies 093 (see step 8), not before.

8. Apply is Joe's. When 093 and the verify script are written and reviewed, stop and hand Joe the file to apply in Studio. After he confirms: verify live with the MCP (to_regclass for the six tables, to_regprocedure for the seven functions, pg_policies count per table), run the verification script in Studio (Joe pastes the NOTICE output) or via the MCP if it can run a transaction that rolls back, then regenerate src/lib/database.types.ts (typegen-check CI enforces the match; see docs/CONVENTIONS.md) and commit it.

9. Gates: root npm run lint (0 errors), npm test, npm run build; native npx tsc --noEmit (the types file is shared through the junction); workers/api npm run check.

10. Docs, in the same PR: videx-wiki/wiki/entities/codebase/database-schema.md (the six tables, the helper, the policies), migrations.md (093 row, applied date), event-taxonomy.md (household_joined), a "Membership RLS" section on database-schema.md or a new concept page under wiki/concepts/architecture if one for RLS patterns already exists (grep for it; do not create a duplicate), parking-lot.md (IN-GR-043 closed with the PR; IN-GR-009 gains a line on what 093 does for shared devices), log.md. Leave the G2 plan's §11 to the strategy thread.

11. One PR; merge is Joe's. End with a summary for the strategy thread containing, verbatim: the five RPC signatures and their error codes, the policy names, the §8 answers, the IN-GR-043 scan result, and anything you found that changes H2 or H3 as the plan describes them (their handoffs are written from this summary).

Out of scope: any native or Worker code beyond the event-name constant; the list screen, list scope, share arm (H2); the Worker list page, pending-link intent, referrer key (H3); notification changes and migration 094 (H4); Realtime; any change to the personal watchlist table or src/lib/storage/watchlist.ts.
```
