# Handoff: Growth G2 session H2, the household in the app (list, items, reactions, invite share)

**Date:** 2026-09-18 · **Source:** [G2 plan](2026-09-17-004-feat-phase-g2-household-loop-plan.md) §3, §4 H2, §9 (all accepted), §11b (the H1 contract; read it first) · **Size:** L, native app plus shared-lib helpers · **Runs in parallel with:** H3 (links, `2026-09-18-004`); file ownership below is strict. **After:** H1 merged (PR #211) and migration 093 live. **D16 (member removal) is Joe's call; step 7 is conditional on it.**

**Joe, before starting:** answer D16 in plan §11b. **During the session:** if D16 is (a), apply migration 094 in Studio when the session hands it over (never `db push`); regenerate types is the session's. Device smoke at the end is yours via OTA (two accounts on one phone in sequence, then two phones in H5).

---

```
Context: Videx. Live RN/Expo 56 app in native/ (expo-router, NativeWind, react-query; native/src/lib is a junction to the shared src/lib; never recursive-delete inside native/). Supabase with migration 093 live (households, household_members, watchlists, watchlist_items, watchlist_reactions, household_invites; membership RLS; RPCs). Read first, in this order: docs/plans/2026-09-17-004-feat-phase-g2-household-loop-plan.md §3, §4, §9 and §11b (the RPC contract and error codes you build against; do not re-derive them), supabase/migrations/093_households.sql (§1 tables and grants, §3 RPCs), videx-wiki/wiki/entities/codebase/rpcs.md and wiki/concepts/techniques/rls-pattern.md (the membership section), src/lib/database.types.ts (households* types), native/src/app/(tabs)/watchlist.tsx (two status segments today), native/src/hooks/useWatchlist.ts and useSharedRoom.ts (the react-query patterns), native/src/components/WatchlistActions.tsx (the signed-out and initialising guards; add beside them, do not restructure), native/src/components/DetailEngagement.tsx (thumbs write the personal rating; reactions are separate), native/src/components/ShareButton.tsx (ShareTarget's two arms; you add a third), src/lib/growth/shareCopy.ts (withShareAttribution hardcodes via=share), src/lib/growth/growthEvents.ts (GROWTH_EVENT_NAMES has household_joined from H1; the union member may not), native/src/app/profile/[section].tsx and native/src/components/profile/* (where household management lives), native/src/app/room/[id].tsx (a screen that resolves an object id and calls clearPendingLinkFor: the shape your list screen follows), videx-wiki/wiki/concepts/product/tone-and-voice-guide.md, docs/CONVENTIONS.md, videx-wiki/AGENTS.md. Worktree branch off main named feat/growth-h2-household-app, relative paths.

File ownership (H3 runs at the same time). Yours: native/src/app/list/** (new), native/src/app/(tabs)/watchlist.tsx, native/src/components/WatchlistActions.tsx, native/src/components/DetailEngagement.tsx, native/src/components/ShareButton.tsx, native/src/components/household/** (new), native/src/components/profile/** and profile/[section].tsx, native/src/hooks/useHousehold*.ts (new), src/lib/household/** (new), src/lib/growth/shareCopy.ts, src/lib/growth/growthEvents.ts, supabase/migrations/094_* (only under D16 (a)). Not yours, do not touch: workers/api/**, src/lib/growth/inboundLink.ts, installReferrer.ts, native/src/pendingLink.ts, native/src/app/+native-intent.tsx, auth.tsx, curating.tsx, providers/notifications.tsx. If you need a change there, write it in the summary for H3.

Contract with H3 (both sides build to this, nobody waits): the list screen is native/src/app/list/[id].tsx and accepts an optional invite query param: /list/{listId}?invite={token}. H3 makes universal links, app links, the Play referrer and the pending link deliver that route with the param. Your screen handles it (step 3). Signed-out preview data comes from H3's public Worker route GET /v1/list/:id/preview, JSON { id, name, household_name, count, posters: string[] (up to 6 poster paths), members: number }, cached 60s, never any member names or the token. Until H3 lands, stub it behind a small fetch helper in src/lib/household/listPreview.ts with that exact type and a test, so the screen compiles and the real route drops in.

Goal: a person can create a household, invite by link, see the shared list beside their own, add titles to it from the detail page, react (up, down, tonight), see who added what and who is in the household, and leave. Every RPC error code maps to copy. No nudges (H4), no Worker page (H3).

Decided (do not reopen): one shared list per household in v1, shown as a second segment beside the personal list; household names 1 to 40 characters; up to 6 members; invite is a link (share sheet), never a typeable code; reactions up / down / tonight, one per member per item, no comments; "watched" stays personal; shared-list items are not excluded from For You (D8); events per D13.

Do, in this order:

1. Shared lib, pure, tested under the root vitest rig, in src/lib/household/: (a) errors.ts: householdErrorCode(error: unknown): HouseholdErrorCode | 'rls_denied' | 'unknown', reading error.message for the exact code and error.code === '42501' for RLS, and householdErrorCopy(code) with tone-guide copy for every code in plan §11b (invite_expired: "This invite has expired. Ask for a new one."; household_full: "This household is full."; etc., no exclamation marks, no em dashes); (b) rpc.ts: typed wrappers over supabase.rpc for the five RPCs that unwrap data[0] and throw a HouseholdError carrying the code; (c) items.ts: the insert shape (ON CONFLICT DO NOTHING via .upsert({ ...}, { onConflict: 'watchlist_id,tmdb_id,media_type', ignoreDuplicates: true })), title clipped to 300 and poster_path to 200 before the write, and the reaction upsert; (d) listPreview.ts (the H3 contract type and fetch). Tests for the code mapping, the clipping and the preview type.

2. Hooks in native/src/hooks/: useHousehold() (the caller's memberships via household_members under RLS, plus households and their one watchlist; react-query key ['native','household']), useSharedList(listId) (items with reactions, key ['native','sharedList', listId]; refetch on focus; invalidate on every write), useHouseholdMembers(householdId) (household_members_view). Signed out: all disabled, no network.

3. The list screen native/src/app/list/[id].tsx. Signed in and a member: header (list name, household name, member count with a tap to the members screen), items newest first with poster, title, "added by {username}" and the three reactions as toggles (own reaction highlighted; counts per reaction), "tonight" items pinned to a "Tonight?" strip at the top when any exist, long-press or swipe to remove an item the caller may delete (adder or owner), share button (step 5), empty state ("Nothing here yet. Add a title from any detail page."). With ?invite= and signed in: call join_household(token) immediately; on success (already_member true or false) call clearPendingLinkFor('/list/{id}') and show the list; when already_member is false emit household_joined (step 6) and show a one-line toast ("You're in."); on an error code show the copy with a Back action and do not clear the pending link (a retry after sign-in must still work). Signed out: render the preview from listPreview (name, household name, count, posters) with "Join" → router.replace('/auth') (H3's pending-link resume brings the person back here with the param after sign-in or sign-up; you do not need to store anything). Not a member and no invite: the same preview with "Ask for an invite link" copy and no Join.

4. Watchlist tab: a segmented picker above the existing status segments: "Mine" and, for each household the caller belongs to, its name. The personal list is untouched. The shared segment renders the shared list inline (reuse the list screen's item component, not a navigation). A person in no household sees a single quiet row under the picker: "Watch together. Create a household and share one list." → household creation. Detail page: WatchlistActions gains "Add to {household name}" beside the personal add when the caller is in exactly one household (a small picker when in several), same guards as the personal add (signed out → sign-in with the pending link, initialising → disabled). No change to DetailEngagement's thumbs.

5. Sharing: ShareTarget gains a third arm { listId, listName, householdName, count } and ShareButton renders it with copy from a new buildListShareCopy in shareCopy.ts: "{household name}: {count} titles to pick from together.\n{url}" where url is https://videxstreaming.com/list/{listId}?invite={token}&via=household. Minting: the share tap calls create_invite(household_id) (owner) and builds the URL; a member who is not the owner shares the plain list URL without a token (with copy "Ask {owner} for an invite" if they try to invite; keep it simple: members see Share, owners see Invite). withShareAttribution takes a via parameter (default 'share'; pass 'household' here); update its callers and tests. Events: share_initiated / share_completed with object { type: 'list', id: listId }, via 'household', metadata { surface: 'list' }.

6. Events: add the household_joined union member to GROWTH_EVENT_NAMES' type (H1 added the name to the list and the CHECK): { name: 'household_joined'; object: { type: 'list'; id: listId }; via; src; metadata: { household_id } }. Emit it only when join_household returned already_member = false. Keep the Worker's own list in sync if it has one (grep workers/api/src/growthEvents.ts; if it needs the name, say so in the summary for H3 rather than editing it).

7. Household management under Profile → a new "Household" section (profile/[section].tsx pattern): create (name field, create_household), the members list (household_members_view: username, role, joined date), Invite (owner: create_invite → share sheet with the step-5 copy), Leave (leave_household; confirm dialog; copy explains what happens to items you added), and, ONLY IF Joe chose D16 (a): write supabase/migrations/094_household_remove_member.sql with remove_member(p_household_id, p_user_id) (owner only; codes not_owner, not_member, cannot_remove_self; reuses household_leave_internal for the removed user) and revoke_invite(p_household_id) (owner only; sets revoked_at on the open invite), SECURITY DEFINER, search_path pinned, EXECUTE to authenticated only, plus a verify script in the 093 style; Joe applies it; regenerate types; then "Remove" on each member row (owner only, confirm) and "Revoke invite link" under Invite. If Joe chose (b) or (c), skip the migration and say so.

8. Gates: root npm run lint (0 errors), npm test, npm run build; native npx tsc --noEmit and npm run lint (root npm install first). No Worker changes.

9. Docs: videx-wiki/wiki/entities/codebase/hooks.md (new hooks), event-taxonomy.md (household_joined emitted where), a "Household" section on the screens or native entity page that exists (grep for the watchlist tab's entry), rpcs.md (094 if written), registers untouched unless you file something, log.md. Leave plan §11 to the strategy thread.

10. One PR; merge is Joe's. Before merge, Joe smoke-tests by OTA on one phone with two accounts in sequence: A creates a household and adds a title; A shares the invite link; B opens it (with H3 merged) or B pastes videx://list/{id}?invite={token} (without H3), joins, sees the title and reacts; A sees the reaction; B leaves. End with a summary for the strategy thread: what shipped, the D16 outcome, anything H3 must change (the pending-link route and param, any Worker event name), and what H5 must test.

Out of scope: nudges and notification preferences (H4), the Worker list page and JSON (H3), inbound-link parsing, the pending link, the referrer (H3), Realtime, any change to the personal watchlist table or src/lib/storage/watchlist.ts, "For both of you" (G5).
```
