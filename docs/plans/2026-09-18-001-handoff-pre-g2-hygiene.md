# Handoff: pre-G2 hygiene (IN-GR-022, IN-GR-025, IN-GR-045)

**Date:** 2026-09-18 · **Source:** strategy thread, after the v2.5.0 tag; parking-lot rows filed by Growth S3, S4 and the code sweep · **Size:** S to M, one refactor PR with no behaviour change beyond the push copy · **Runs in parallel with:** the IN-GR-034 session (`2026-09-17-002`); the two share no files. **Order:** before G2 H3 (which adds a Worker route and a POST handler to `index.ts`) and before H4 (which adds a second push function beside `send-notifications`). Neither G2 session should inherit the duplication.

**Why now.** The sweep filed a cleanup batch it deliberately did not build (a review-then-fix session must not refactor). G2 builds on exactly the surfaces in that batch: H3 adds `/list/:id` and a JSON route to a Worker whose 404s, TTLs and body reading are hand-copied per route, and H4 extracts push sending into `_shared` next to a type error that already fails `deno check`. An hour of tidying now saves each G2 session from either copying the mess or fixing it mid-feature.

**Joe, before starting:** nothing. During the session you deploy `send-notifications` once the copy change is reviewed (`npx supabase functions deploy send-notifications`, keep `verify_jwt = true`); the type fix in `_shared/userScope.ts` rides the next deploy of any function that imports it, which is fine.

---

```
Context: Videx. Shared lib in src/lib (native/src/lib is a junction to it; never recursive-delete inside native/), live RN/Expo 56 app in native/, Cloudflare Worker in workers/api (Hono), Supabase Edge Functions in supabase/functions (Deno; deploys are Joe's). Read first: videx-wiki/wiki/registers/parking-lot.md rows IN-GR-022, IN-GR-025, IN-GR-045 (the scope, verbatim), docs/v2/phase-summaries/phase-growth-code-sweep-summary.md (why these were filed and not fixed), docs/CONVENTIONS.md, videx-wiki/wiki/concepts/product/tone-and-voice-guide.md (no em dashes, never the word AI), videx-wiki/AGENTS.md. Worktree branch off main named chore/pre-g2-hygiene, relative paths.

Goal: one PR that removes the duplication the code sweep filed as IN-GR-045, fixes the deno check failure (IN-GR-022) and aligns push copy with the share copy (IN-GR-025). This is a refactor with a test net: every change except the push copy is behaviour-preserving, and the existing tests must prove it. No new features, no renames of public routes, no schema, no new dependencies.

Do, in this order, one commit each and each green:

1. IN-GR-022, supabase/functions/_shared/userScope.ts lines 126 and 128: PostgrestFilterBuilder<any, any, any[]> no longer matches the esm.sh postgrest-js types, which now take more type arguments. Pin the esm.sh import to the version send-notifications and embed-query actually resolve (check their import maps or URLs; use the same pinned version in all three places) and give the builder the argument count that version wants. Gate: deno check on send-notifications, embed-query and revoke-apple-token all pass. Do not change the runtime code of userScope.ts.

2. IN-GR-025, supabase/functions/send-notifications/compose.ts: (a) the push copy keeps its own service-label map because the Deno function cannot import src/lib. Keep the local map but add a vitest under supabase/functions/send-notifications/__tests__ (already in the root vitest include list) that imports both maps and asserts they are equal key for key, so a drift fails CI; if compose.ts cannot be imported by vitest because of Deno-only imports, split the map into a dependency-free module the test can import from both sides. (b) Remove em dashes from every push body and title. Single-title arrival today reads "Now on Apple TV+ — on your watchlist."; rewrite as "Now on Apple TV+, from your watchlist." and treat the other templates the same way, keeping length under the platform limits the function already respects. Show the before and after copy in the PR description for Joe to approve; do not invent new messages. Update the compose tests. Joe deploys after review.

3. IN-GR-045 Worker (workers/api/src/index.ts and the page modules): (a) the four hand-built 404 responses become one notFound(c, kind) helper that keeps the exact status, content type, cache headers and body of each existing 404 (write a test that snapshots the four before touching them); (b) page TTL and cache-key construction duplicated per page type (title, room) move to one helper in pageShell.ts or a small cache.ts, same keys and same TTLs, so PAGE_CACHE_VERSION stays the single knob; (c) the JSON body-reading boilerplate copied between POST /v1/share/room and POST /v1/growth/events becomes one readJsonBody(c, { maxBytes }) that preserves the current error statuses (411 without content-length, 413 over the cap, 400 invalid JSON) and is covered by a test for each status; (d) titlePage.ts stops re-exporting pageShell; import from pageShell directly where needed. Do not touch route paths, response bodies or cache keys: run the existing Worker tests before and after and diff any snapshot. If a page module changes in a way that alters output bytes (it should not), bump PAGE_CACHE_VERSION and say so.

4. IN-GR-045 shared lib and native: (a) src/lib/growth/installReferrer.ts validates a referrer by formatting and re-parsing a URL; replace with a direct check on the parsed fields with the same accept and reject set (the tests in src/lib/growth/__tests__/installReferrer.test.ts define it; add a case for each branch you touch); (b) month tables and pluralisation live in three places (find them: grep for the month names and for a titles/title ternary); move to src/lib/format or an existing utils module with tests, and import from the three call sites; (c) username availability and save are duplicated across native/src/app/choose-username.tsx and native/src/components/profile/ProfileAccount.tsx; extract one hook (native/src/hooks/useUsernameSave.ts or similar) that both screens call, preserving the current error messages and the checkUsernameAvailable-throws behaviour from the sweep. No visual change on either screen.

5. Gates, all green before the PR: root npm run lint (0 errors), npm test, npm run build; native npx tsc --noEmit and npm run lint (root npm install first); workers/api npm run check and its tests; deno check on the three functions above. IN-GR-022 must no longer be listed as a known failure anywhere (grep the docs and workflows for it).

6. Docs: close IN-GR-022, IN-GR-025 and IN-GR-045 in videx-wiki/wiki/registers/parking-lot.md with the PR number and one line each on what changed; update the parking-lot counts line; append videx-wiki/log.md. If the Worker cache helper changes where PAGE_CACHE_VERSION lives, update the Worker entity page under videx-wiki/wiki/entities/codebase accordingly. One PR; merge is Joe's. State in the PR description which change needs an Edge Function deploy (the two function edits) versus a Worker deploy (automatic on merge) versus an OTA (the native hook extraction is JS-only; it can ride the IN-GR-034 OTA rather than its own).

Out of scope: IN-GR-037 to IN-GR-039 (efficiency, measure first), IN-GR-040 and IN-GR-044 (resume path, folded into G2 H3), IN-GR-041 (folded into H4), IN-GR-043 (folded into H1 with migration 093), anything in src/lib/recommendations-v2, search or sync.
```
