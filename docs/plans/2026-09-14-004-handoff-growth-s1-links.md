# Handoff: Growth session S1, links (G0-1 to G0-4)

**Date:** 2026-09-14 · **Source:** [G0/G1 plan](2026-09-14-003-feat-phase-g0-g1-growth-foundations-and-sharing-plan.md) §3, §4 (G0-1 to G0-4), §6 (088), §9, §13 · One session, one PR. S2 (attribution) and S3 (sign-in) start after this merges and run in parallel; their prompts are written after this session's summary.

**Joe, before starting (plan §9c, S1 rows):** Play App Signing status and certificate SHA-256; App Store approval status and listing URL; Cloudflare dashboard routes for `videxstreaming.com/.well-known/*`, `/room/*`, `/list/*`, `/delete-account*`. The session can build everything without these, but the `.well-known` files need the fingerprint and the smart banner needs the listing status before the PR is complete.

---

```
Context: Videx. Shared engine and lib in src/lib (native/src/lib is a junction to it; never recursive-delete inside native/), live RN/Expo 56 app in native/ (expo-router, file routes under native/src/app), Cloudflare Worker in workers/api (Hono; pure modules unit-tested by the root vitest), Supabase migrations in supabase/migrations (apply is Joe's action in Studio, never db push). Read first, in this order: docs/plans/2026-09-14-003-feat-phase-g0-g1-growth-foundations-and-sharing-plan.md (all of it; §3, §4 G0-1 to G0-4, §6 row 088, §9 and §10 are binding), docs/CONVENTIONS.md, workers/api/README.md (route rule), videx-wiki/AGENTS.md, videx-wiki/wiki/concepts/forward-planning/growth-loops.md. Load the cloudflare and workers-best-practices skills before touching the Worker. Work on a worktree branch off main named feat/growth-s1-links with relative paths.

Goal: every object Videx can share has a canonical https URL on videxstreaming.com that renders a crawler-readable preview, opens the installed app directly on iOS and Android, and lands the recipient on the object after sign-in or sign-up. This session delivers the links; it does not touch sign-in providers (S3), telemetry (S2) or share copy (S4).

Decisions already taken (do not reopen): title URL is /t/{movie|tv}/{tmdbId}-{slug}, resolved by type and id only, slug cosmetic; rooms are shared as snapshots in a new shared_rooms table (never a live mood_rooms.id); /list/{id} is reserved only (grammar and mapping, branded 404, no screen); the SPA gets no router (PLAT-1 D5 stands); .well-known is served by the Worker; no feature flag.

1. ADR-015 (videx-wiki/wiki/concepts/decisions/adr-015-object-urls-and-inbound-links.md, mirror the format of adr-014): object URLs are Worker-owned on the web and Expo Router-owned on device; the SPA is not a distribution surface; URL grammar for /t, /room, /list with the ?via= and ?src= query contract (via = URL channel: share|push|seo|card|household; src = originating session on shares: push|organic); snapshot rule for rooms; the inbound mapping contract below. Update registers/open-questions.md (D5 row), index.md, log.md. Open the IN-GR family in registers/parking-lot.md with any follow-ups you file.

2. Slug and title page (workers/api/src/titlePage.ts + index.ts). Add a pure titleSlug(title, year) in src/lib/growth/slug.ts (lowercase, ascii-fold, [^a-z0-9]+ to '-', trim, append -year when present, max 80 chars; reuse the shape of buildChannel4Slug in src/lib/deepLinks.ts) with tests. The Worker route becomes /t/:type/:ref where ref is {tmdbId} or {tmdbId}-{slug}; parse the leading digits, 301 to the canonical slugged URL when the suffix is missing or stale, canonical link and og:url use the slugged form, CANONICAL_ORIGIN unchanged. Extract the shared HTML shell (head, CSS, security headers) from titlePage.ts into pageShell.ts so roomPage.ts can reuse it; policyPages.ts may stay as is. Add <meta name="apple-itunes-app" content="app-id=6785395342, app-argument={canonical}">. If Joe confirms the App Store listing is approved, set APP_STORE_URL and IOS_APP_STORE_LIVE (single-const switch, tests already assert the iOS CTA). Pass ?via= and ?src= through unchanged into the videx:// deep link and into the Play URL as &referrer=via%3D...; assert in a test that the edge cache key still ignores the query (index.ts:281 pattern). Do not add any event logging (S2).

3. Room snapshots. Migration supabase/migrations/088_shared_rooms.sql exactly as plan §6 row 088 (columns, RLS on with no anon policy, index, delete_own_account and export_user_data extended in the same file; verify the current function bodies live in 044 and 061 before rewriting them). Worker POST /v1/share/room (Bearer Supabase JWT via the existing verifySupabaseJwt, rate-limited like /v1/foryou): body { kind: 'global'|'anchor', source_ref, label, description?, titles: [{tmdb_id, media_type}] max 60 } validated in a pure sharedRooms.ts (strip personal framing: a label starting "Because you liked X" or "If you love X" becomes "More like X"), service-role insert, returns { id, url } with url = https://videxstreaming.com/room/{id}. Worker GET /room/:id renders roomPage.ts from the row plus a titles read (title, release_year, poster_path) and, for the poster grid, the first 12 posters; OG tags: og:title = label, og:description = "{n} titles picked for the mood, with UK availability on Videx", og:image = first poster; a "picked on {date}" line; "Open in the Videx app" -> videx://room/{id}; the same UA-aware store CTA as the title page; branded 404 for unknown ids; 24h edge cache keyed by id and platform bucket. Worker GET /list/:id returns the branded 404 page (noindex) for now.

4. Well-known files (workers/api/src/wellKnown.ts + routes). GET /.well-known/apple-app-site-association: JSON, content-type application/json, no redirect, { applinks: { details: [{ appIDs: ["CT8F3578W8.app.videx.streaming"], components: [{ "/": "/t/*" }, { "/": "/room/*" }, { "/": "/list/*" }] }] } } (also include the legacy "apps": [] and "paths" form for older iOS). GET /.well-known/assetlinks.json: relation delegate_permission/common.handle_all_urls, package_name app.videx.streaming, sha256_cert_fingerprints containing the upload key and the Play App Signing key Joe supplies (read both from wrangler [vars] ASSETLINKS_FINGERPRINTS, comma-separated, so a key rotation is a config change). Tests assert exact JSON and headers. Add the routes to workers/api/README.md's route list and note that Joe registers the dashboard routes.

5. Native config (native/app.json): ios.associatedDomains ["applinks:videxstreaming.com"]; android.intentFilters one entry, action VIEW, autoVerify true, category BROWSABLE + DEFAULT, data [{ scheme: "https", host: "videxstreaming.com", pathPrefix: "/t/" }, { ..."/room/" }, { ..."/list/" }]. Confirm native/app.config.js leaves both untouched for the dev variant. Do not bump versions; Joe runs the builds.

6. Inbound mapping (src/lib/growth/inboundLink.ts, pure, tests). parseInboundLink(path: string): { route: string; object: { type: 'title'|'room'|'list'; id: string } | null; via: string | null; src: string | null }. Accepts the https path forms above (with or without slug, with or without query), the videx://detail/{type}-{id}, videx://room/{id}, videx://watchlist and videx://reset-password forms (pass the last two through unchanged), and anything else -> route '/' with object null. Route outputs: /detail/{type}-{id}, /room/{id}, /list/{id}. native/src/app/+native-intent.tsx: run stripMalformedQuery first (keep the IN-DEP-001 guard and its comment), then the mapper, then write native/src/pendingLink.ts (MMKV, pattern of onboardingDraft.ts: { route, object, via, src, seenAt }, 24h TTL, consumed once) and return the mapped route. Consumers: native/src/app/auth.tsx after a successful sign-in replaces to the pending route instead of "/"; native/src/app/curating.tsx replaces to the pending route instead of /(tabs)/foryou when one exists; native/src/app/(tabs)/_layout.tsx leaves the redirect logic alone (the pending record survives the /auth bounce). Clear the pending record on sign-out alongside the onboarding draft.

7. Room screen native/src/app/room/[id].tsx: fetches the snapshot through a new Worker GET /v1/room/:id (JSON; the public page and the app share one read path) and renders the frozen titles with the existing card components and the viewer's own availability chips; header shows the label and "picked on {date}"; a share button in the same top-right position as the detail page (BackButton top-left, insets.top + 12) that for now shares the plain URL (copy is S4). Wire the existing MoodRooms row and the anchored room view to "share this room" by calling POST /v1/share/room then the OS share sheet (reuse ShareButton with a url prop; keep emitShare as is). Register the screen in native/src/app/_layout.tsx.

8. Verify. Root: npm run lint 0 errors, npm test, npm run build. native/: npm run lint, npx tsc --noEmit, npx expo export --platform android. Worker: npm run check in workers/api. Curl the deployed Worker after merge (Joe registers routes first): the two .well-known files, /t/movie/603 -> 301 -> /t/movie/603-the-matrix-1999 with the smart-banner meta, a /room/{id} page, /list/x -> 404 with noindex. Device checks happen in S5, but if Joe has an ad-hoc build to hand, test one universal link cold and warm and one deferred link after sign-out.

9. Docs and wiki from the same worktree: workers/api/README.md; docs/v2/launch/release-runbook.md (associated domains and intent filters now in app.json; both platforms need a rebuild); wiki pages: new concepts/techniques/inbound-deep-linking.md (universal links, app links, the intent interceptor, pending link, snapshot rooms), entities/codebase/migrations.md row for 088, growth-loops.md status line, index.md, log.md. PR footer standard. End with a summary for the strategy thread: what shipped, the curl outputs, what Joe must still do, and anything the plan got wrong.
```
