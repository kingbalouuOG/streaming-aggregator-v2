# Handoff: Growth G2 session H3, list links (Worker preview page, invite param, pending-link intent, referrer)

**Date:** 2026-09-18 · **Source:** [G2 plan](2026-09-17-004-feat-phase-g2-household-loop-plan.md) §2 item 3, §3, §4 H3, §9 D4/D5, §11b (what H1 changed) · **Size:** M, Worker plus shared-lib plus three native files · **Runs in parallel with:** H2 (app, `2026-09-18-003`); file ownership below is strict. **After:** H1 merged and 093 live; the hygiene session (PR #210) merged, so `notFound`, `readJsonBody` and `pageCacheKey` exist. **Folded in:** IN-GR-040 and IN-GR-044 (the resume-path pass).

**Joe, before starting:** nothing. The Worker deploys on merge.

---

```
Context: Videx. Cloudflare Worker in workers/api (Hono; pure page modules under src/ tested by the root vitest rig), shared lib in src/lib (native/src/lib is a junction; never recursive-delete inside native/), live RN/Expo 56 app in native/. Read first, in this order: docs/plans/2026-09-17-004-feat-phase-g2-household-loop-plan.md §2 item 3 (the audit of every file you touch), §3, §4 H3, §11b (the H1 contract: joins are idempotent for members; the list screen clears the pending link, not you), videx-wiki/wiki/concepts/decisions/adr-015-object-urls-and-inbound-links.md, workers/api/src/index.ts (the /room/:id route, recordPreview, withAttribution, withEdgeCache, the /list/:id 404 stub), workers/api/src/roomPage.ts and roomStore.ts (the template for a public object page and its loader), workers/api/src/pageShell.ts (pageCacheKey, PAGE_TTL_SECONDS, PAGE_CACHE_VERSION, applyAttribution and the two markers), workers/api/src/notFound.ts, src/lib/growth/inboundLink.ts (parseInboundLink: the /list/{id} branch routes to '/' today), native/src/pendingLink.ts (VERSION 1, no intent), native/src/app/+native-intent.tsx, native/src/app/auth.tsx (the focus effect that resumes a pending link) and curating.tsx (the one-shot consumer), src/lib/growth/installReferrer.ts (keys t= and r= only), native/src/attribution.ts (referrer read on first open), videx-wiki/wiki/registers/parking-lot.md rows IN-GR-040 and IN-GR-044 (yours to close), docs/CONVENTIONS.md, videx-wiki/AGENTS.md. Worktree branch off main named feat/growth-h3-list-links, relative paths.

File ownership (H2 runs at the same time). Yours: workers/api/**, src/lib/growth/inboundLink.ts, src/lib/growth/installReferrer.ts and their tests, native/src/pendingLink.ts, native/src/app/+native-intent.tsx, native/src/app/auth.tsx, native/src/app/curating.tsx, native/src/attribution.ts. Not yours: native/src/app/list/** (H2 builds the screen), native/src/components/**, native/src/hooks/**, src/lib/growth/shareCopy.ts and growthEvents.ts, WatchlistActions.tsx (its route-equality guard from IN-GR-040 stays as it is; note in the summary if your resume change makes it removable). If you need a change there, write it in the summary for H2.

Contract with H2: the app screen is /list/{listId} and accepts an optional invite query param. Your job is that every way in delivers exactly that: a universal or app link https://videxstreaming.com/list/{id}?invite={token}&via=household, the app scheme videx://list/{id}?invite={token}, a cold start through the pending link, and an Android store install through the Play referrer. The screen owns the join and clears the pending link itself after join_household resolves; you never clear a list route on focus. H2's signed-out state reads your GET /v1/list/:id/preview.

Goal: a shared list URL previews in chat apps and browsers like a room URL does, carries the invite into the app on every path, and resumes correctly through sign-up. No app UI, no join logic, no nudges.

Do, in this order:

1. Worker loader and page. workers/api/src/listStore.ts: loadListPreview(client, id) with a service-role read that returns { id, name, household_name, count, posters (up to 6 poster paths, newest items first), members } or null; it must never select usernames, added_by or anything from household_invites. workers/api/src/listPage.ts (pure): renderListPage(data, bucket) modelled on roomPage.ts: OG title "{name} on Videx", description "{count} titles to pick from together, with UK availability on Videx" (countLabel from src/lib/format/plural), poster grid, store CTAs and the smart banner via the pageShell helpers, the deep link videx://list/{id} with DEEP_LINK_QUERY_MARK so applyAttribution fills via and src; add the invite param to the deep link and to the Play referrer (step 4) when present on the request URL. listPageCacheKey(id, bucket) via pageCacheKey. A test asserts the rendered page contains no username, no email and no token even when data carries decoys (put the decoys in the fixture).

2. Routes in index.ts. GET /list/:id: id must be a uuid (else notFound(c, 'list')); withEdgeCache with listPageCacheKey and a 60-second TTL (a live list, not a frozen room; make the TTL a named constant LIST_PAGE_TTL_SECONDS beside PAGE_TTL_SECONDS in pageShell.ts); unknown id → notFound(c, 'list'); recordPreview(c, resp, { type: 'list', id }); withAttribution(resp, via, src, object) so the referrer carries the list; the invite token is NOT part of the cache key and must be re-applied after the cache read (the same way via and src are: a third marker or an extension of applyAttribution; add it to pageShell.ts and bump PAGE_CACHE_VERSION). GET /v1/list/:id/preview: JSON of loadListPreview, public, same 60-second edge cache, 404 JSON when unknown. Do not add the JWT member route the plan listed: H1 made it unnecessary (the app reads under RLS). Keep GET /room/:id byte-identical (the notFound snapshot tests and titlePage tests guard this; if PAGE_CACHE_VERSION moves, update the cache-key tests only).

3. Inbound link. parseInboundLink: /list/{uuid} routes to /list/{id} (not '/') with object { type: 'list', id } and a new field invite: string | null on InboundLink, read from ?invite= (uuid only; anything else is null), on both the https and videx:// forms; a non-uuid list id stays route '/' with no object. Tests for every form plus the decoy cases (invite present on a title link is ignored). +native-intent.tsx: a list link becomes the router path /list/{id}?invite={token} when the token is present.

4. Pending link and resume (IN-GR-040 and IN-GR-044 fold-in). PendingLink VERSION 2 with intent: 'open' | 'join' and invite: string | null; version-1 rows are discarded on read (a 24-hour TTL already bounds them). writePendingLink stores list links deliberately (today it does so by accident; make the branch explicit with a test) with intent 'join' when a token is present, else 'open'. The two consumers (auth.tsx focus effect, curating.tsx one-shot) push the stored route with the query param and never clear a list route themselves; for title and room routes keep today's behaviour. Then the resume pass the sweep filed: key pending-link clearing on the resumed route reaching focus rather than on session change where that is what forced replace-then-push, and replace the replace('/') + setTimeout(push) workaround with router.replace(route, { withAnchor: true }) if Expo Router 6 in this project supports it (check node_modules/expo-router's version and types; if not, leave the workaround and say so); fix the two IN-GR-044 cases: an onboarded account that never chose a name must hit the name gate before a link resumes, and a warm link while /auth is on the stack must not leave two /auth entries. Each of these has a test where the code is testable (pendingLink is; the effects are not: describe the device check instead).

5. Referrer. installReferrer.ts: keys l={listId} and i={token} (uuid each) beside t= and r=; buildPlayReferrer emits them for a list object with an invite; parseInstallReferrer returns route /list/{id}?invite={token} (or without the param) and the list object; a title key still wins over a room key, and a list key wins over both when present. attribution.ts consumes it unchanged (it pushes the route). Tests round-trip every combination.

6. Gates: root npm run lint (0 errors), npm test, npm run build; native npx tsc --noEmit and npm run lint (root npm install first; +native-intent, pendingLink, auth.tsx and curating.tsx are native); workers/api npm run check and its tests. Bump PAGE_CACHE_VERSION once (step 2) and say so.

7. Docs: videx-wiki/wiki/concepts/decisions/adr-015-object-urls-and-inbound-links.md (list grammar now live; invite param; pending-link v2), the Worker entity page under wiki/entities/codebase (routes and TTLs), event-taxonomy.md (preview_fetched / preview_opened for lists), parking-lot.md (close IN-GR-040 and IN-GR-044 with what changed, or leave one open with a reason), log.md. Leave plan §11 to the strategy thread.

8. One PR; merge is Joe's. Before merge, verify with curl against the deployed preview (the Worker deploys from main after merge; before that, wrangler dev or the unit tests) that /list/{unknown-uuid} is the branded 404, /list/{live id} renders with no member names, /v1/list/{id}/preview returns the shape, and the OG tags are present; on device (Joe, by OTA): a shared list link opens the app on the list screen with the invite param cold and warm, a sign-up through the link lands on the list after Curating, and an Android store install from the page carries l= and i= (adb shell am broadcast per the S2 runbook). End with a summary for the strategy thread: routes and TTLs, the PendingLink v2 shape, what H2 must call and when, and what H5 must test.

Out of scope: the list screen and the join call (H2), share copy and the share button (H2), nudges (H4), the room and title pages beyond the shared helpers, Realtime, any migration.
```
