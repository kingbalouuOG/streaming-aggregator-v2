# Handoff: G0/G1 code sweep before G2

**Date:** 2026-09-17 · **Status:** RUN 17 Sept in the strategy thread itself (Joe's call, ahead of IN-GR-034 and the 2.5.0 release); summary `docs/v2/phase-summaries/phase-growth-code-sweep-summary.md` · **Source:** strategy-thread decision after the G0/G1 close (plan §11e) · **Size:** M · **Order (as written):** after the IN-GR-034 session merges (it touches `WatchlistActions.tsx` and `curating.tsx`, which are in this sweep's scope); G2 planning runs in parallel in the strategy thread and does not wait for this.

**Why.** Four build sessions and one device pass landed roughly 40 files in three days, reviewed PR by PR but never as one surface. The device pass found five defects; a code sweep finds the kind a phone cannot (a missing guard on a path no test walked, a Worker input that trusts the client, an RLS gap). Do it once, now, before G2 builds households on top.

---

```
Context: Videx. Shared lib in src/lib (native/src/lib is a junction to it; never recursive-delete inside native/), live RN/Expo 56 app in native/, Cloudflare Worker in workers/api (Hono), Supabase (migrations, Edge Functions). Read first: docs/v2/phase-summaries/phase-growth-g0-g1-summary.md (what shipped, what the plan got wrong), docs/plans/2026-09-14-003-feat-phase-g0-g1-growth-foundations-and-sharing-plan.md §11a to §11e, videx-wiki/wiki/concepts/decisions/adr-015-object-urls-and-inbound-links.md, videx-wiki/wiki/entities/codebase/event-taxonomy.md (growth_events section), videx-wiki/wiki/registers/parking-lot.md (IN-GR family: do not re-find what is already filed), docs/CONVENTIONS.md, videx-wiki/AGENTS.md. Worktree branch off main named chore/growth-code-sweep, relative paths.

Goal: one review of the whole G0/G1 surface as a single change set, then one PR that fixes the confirmed bugs and files the rest. This is a review-then-fix session, not a refactor: no renames, no restructuring, no style passes, no new features. Anything that would change behaviour beyond a bug fix is filed, not built.

Scope (the growth diff): git diff 95186f9...main restricted to src/lib/growth/**, src/lib/instrumentation/sessionOrigin.ts, src/lib/auth/**, native/src/attribution.ts, native/src/installId.ts, native/src/pendingLink.ts, native/src/app/+native-intent.tsx, native/src/app/auth.tsx, native/src/app/curating.tsx, native/src/app/confirm-email.tsx, native/src/app/choose-username.tsx, native/src/app/room/**, native/src/app/(tabs)/_layout.tsx, native/src/providers/auth.tsx, native/src/providers/notifications.tsx, native/src/components/ShareButton.tsx, native/src/components/WatchlistActions.tsx, native/src/components/auth/**, native/src/components/onboarding/**, native/src/components/profile/**, native/src/hooks/useCompleteOnboarding.ts, native/src/hooks/useUsernameChosen.ts, native/modules/play-install-referrer/**, workers/api/src/** (index growth routes, growthEvents, uaClass, wellKnown, sharedRooms, roomPage, pageShell, titlePage, resetBridge), supabase/functions/send-notifications/**, supabase/functions/revoke-apple-token/**, supabase/migrations/088_*, 089_*, 090_*. Out of scope: the engine trees, search, sync, anything not in that diff.

Method, in this order:
1. Run the code-review skill at high effort on the scope in three passes so findings stay readable: (a) Worker + Edge Functions + migrations, (b) shared lib + native providers and app routes, (c) native components and hooks. Report only; no --fix.
2. Run the security-sentinel agent on (a) with these questions: can any client-supplied field reach growth_events unvalidated; can user_id ever come from the body; is the install-id rate limit bypassable in a way that costs money rather than table noise (IN-GR-008 is filed; go beyond it); does POST /v1/share/room let a caller snapshot titles it cannot see or exceed the size cap; do the .well-known responses ever vary by request; does the /reset bridge accept a type it should not; does revoke-apple-token verify the id_token subject before revoking and refuse a token for another user; does anything log a token, an email or an IP. Then the data-integrity-guardian agent on 088 to 090 and the 086/088/090 rewrites of delete_own_account and export_user_data: every user-scoped table covered, RLS and grants as the migrations claim (verify live with the Supabase MCP, read-only), retention cron present, no policy that lets anon or authenticated read growth_events or shared_rooms directly.
3. Run the kieran-typescript-reviewer agent on (b) and (c) for type safety and dead paths, and the julik-frontend-races-reviewer agent on the three places that race by design: auth.tsx's focus effect and pending link, notifications.tsx cold-start handling and sessionOrigin's 10-second grace, ProfilePrivacy's delete → replace('/auth') against the tabs guard redirect (plan §11e note 2).
4. Triage every finding into: bug (fix here), risk (file as IN-GR-036 onward with a repro), style or opinion (drop, say so in the summary). A bug needs a failing test first where the code is testable; the shared lib and Worker pure modules are, native components mostly are not.
5. Fix the bugs in small commits, one concern each, each green: root npm run lint (0 errors), npm test, npm run build; native npm run lint and npx tsc --noEmit; workers/api npm run check; deno check on both functions (IN-GR-022 is a known pre-existing failure in _shared/userScope.ts; do not fix it here, note whether it still stands). If a fix touches the Worker pages, bump PAGE_CACHE_VERSION. If a fix touches an Edge Function, say so: Joe deploys.
6. Report: docs/v2/phase-summaries/phase-growth-code-sweep-summary.md with the findings table (source, file, severity, verdict, where it went), the fixes, and anything that needs a rebuild versus an OTA. Wiki: parking-lot rows for the filed items, log.md. One PR. End with a summary for the strategy thread: bugs fixed, risks filed, whether anything found changes the G2 readiness view in the phase summary.
```
