# Handoff: Growth session S5, verification (G1-5)

**Date:** 2026-09-16 · **Source:** [G0/G1 plan](2026-09-14-003-feat-phase-g0-g1-growth-foundations-and-sharing-plan.md) §5 G1-5, §7, §11a to §11d · **Follows:** S1 to S4 all merged (16 Sept). This is the first time any of G0/G1 runs on a phone. The session drives seeding, queries, small fixes and the docs; Joe drives the devices. Expect it to span more than one sitting.

**Joe, before starting (in this order):**

1. **Version.** Bump `native/app.json`: `version` 2.4.0 → **2.5.0** (the growth release), `ios.buildNumber` 12 → 13, `android.versionCode` 15 → 16. One commit on main.
2. **Builds.** `ios-release.yml` with `profile=production` (TestFlight, for testers) and again with `profile=preview` (ad-hoc, for your iPhone: TestFlight delivery on it is broken). `android-release.yml` with `submit=true` (Play internal) and install the `videx-release-apk` artifact on your Android. The 16 Sept build-only run already proved the Kotlin referrer module compiles.
3. **Testers update** to the new build (TestFlight and Play internal).
4. **Flip Confirm email on** in Supabase Auth → Providers → Email, only after step 3. Older builds cannot create new email accounts from then on; existing sessions are unaffected.
5. **Store privacy forms.** File the S2 rows (Apple: Device ID, Linked to You, Analytics; Play: Device or other IDs) and the S3 rows (Name, Email Address under Contact Info) from `docs/legal/store-privacy-disclosures.md`. Needed before the App Store submission of this build, not for TestFlight.
6. **IN-GR-002.** If the App Store listing is approved, give the session the listing URL.

---

```
Context: Videx. Live RN/Expo 56 app in native/ (native/src/lib is a junction to the shared src/lib; never recursive-delete inside native/), Cloudflare Worker in workers/api, Supabase (migrations 088 shared_rooms, 089 handle_new_user placeholder, 090 growth_events all applied; Edge Functions send-notifications v8 and revoke-apple-token v1), growth telemetry in growth_events. Read first, in this order: docs/plans/2026-09-14-003-feat-phase-g0-g1-growth-foundations-and-sharing-plan.md (§5 G1-5, §7 test approach, §11a to §11d session outcomes and their "carry into S5" items), docs/strategy/briefs/h0-device-test-checklist.md (the S4 block and item 11), docs/v2/phase-summaries/phase-growth-s1-summary.md and phase-growth-s4-summary.md, videx-wiki/wiki/registers/parking-lot.md (IN-GR family), videx-wiki/wiki/concepts/techniques/inbound-deep-linking.md, videx-wiki/wiki/concepts/architecture/notifications-v1.md, videx-wiki/AGENTS.md, and the memory note reference_ios_device_testing (Joe's iPhone takes ad-hoc builds only; expo-updates can push JS-only fixes in about a minute via ota-update.yml). Worktree branch off main named feat/growth-s5-verification, relative paths. Use the Supabase MCP read-only for evidence queries; any write to production data (seeding a test arrival, deleting test rows) is proposed to Joe first with the exact SQL.

Goal: prove G0 and G1 end to end on real phones, on the 2.5.0 build, and leave a written record. Every check below is pass/fail with evidence: the growth_events rows it produced (select event_name, via, src, object_type, object_id, delivery_id, platform, ua_class, metadata, occurred_at from growth_events where occurred_at > now() - interval '1 day' order by occurred_at), a screenshot where the check is visual, or a log line. A failed check is a defect: fix it in this session when it is small and JS-only (one PR, OTA to the preview channel so Joe can re-test without a rebuild), otherwise file it as IN-GR-028 onward with a handoff-ready description. Never re-verify what the summaries already verified on the Worker; this session is about phones.

Sequencing gate: confirm with Joe that the 2.5.0 builds are installed on his iPhone (ad-hoc) and Android (APK), that testers have updated, and that Confirm email is ON, before running any sign-up check. Link, share and push checks do not need the flip.

Matrix (run on both platforms unless stated; tick each in the checklist file and paste evidence into the PR):

A. Links (S1). A1 universal/app link from WhatsApp, Messages and Slack with the app installed: cold start and warm, https://videxstreaming.com/t/movie/550-fight-club-1999?via=share opens the detail page directly with no browser flash; Android: adb shell pm get-app-links app.videx.streaming shows verified for videxstreaming.com. A2 bare or stale slug link still opens the title. A3 a /room/{id} link (create one by sharing a room) opens the room screen with the same titles the sharer saw; as a non-user on a second phone or a browser, the page shows the preview, poster grid and store CTA. A4 pending link: sign out, tap a link, sign in with email, land on the object; Back goes to the tabs, not a dead end. A5 IN-GR-004: password-reset link cold start and a cold notification tap still work with the tabs beneath. A6 preview rendering: paste a title link and a room link into WhatsApp, iMessage and Slack; poster, title and the availability line render; screenshots.

B. Attribution (S2). B1 first_open once per install (fresh install, then relaunch: exactly one row; an updated 2.4.0 → 2.5.0 install shows prior_install=true). B2 link_opened with via=share for A1. B3 Android store path: uninstall, open a shared page in Chrome, tap Get Videx, install from Play internal, first launch: the app lands on the shared title (referrer t=movie-550) and first_open carries the touch; this is the IN-GR-005 runtime check. B4 signup_completed carries the first-touch via and first_home_view metadata has via; onboarding_events row shows it. B5 preview_fetched vs preview_opened for the links you pasted in A6 (WhatsApp fetches, your tap opens).

C. Sign-in (S3; after the Confirm-email flip). C1 Apple on iPhone: new account (name step prefilled, choose a name, onboarding continues at Connect Services), returning account, Hide My Email. C2 Google on iPhone (a nonce error means Skip nonce check is off) and on Android. C3 provider sign-up from a shared link: onboarding → Choose your name → Curating → the shared title opens (IN-GR-012). C4 email sign-up with Confirm email on: "Check your email", the link opens the app via the /reset bridge and continues onboarding; resend and change-email paths; IN-GR-021 noted if the link is opened on a computer. C5 delete an Apple-linked account on iPhone: Apple sheet → revoke 200 → account deleted (function logs on failure: not_configured or apple_token_exchange_failed). C6 an existing 2.4.0 email account signs in unchanged.

D. Sharing and push (S4). Run S4-1 to S4-11 from docs/strategy/briefs/h0-device-test-checklist.md exactly as written: title copy for subscription, rent-or-buy-only and unavailable titles in WhatsApp and Messages; room share from a card and from the room screen; seeded arrival push → notification_opened with delivery_id → "Tell someone" button and banner with the arrival copy → share carries src=push; leaving-soon copy with the date; bundle push lands on the watchlist with no banner; dismiss persists; six minutes in the background clears the origin; relaunch does not replay (IN-GR-027). The 20-hour cap applies per account, so plan the three push checks across the test accounts or across days. Note completion is iOS-only evidence (Android reports dismiss as shared).

E. Register and cleanup. Close or update every IN-GR row the matrix touches (004, 005 runtime, 012, 021, 027; 002 if the listing URL arrived: flip APP_STORE_URL and IOS_APP_STORE_LIVE in workers/api/src/pageShell.ts, bump PAGE_CACHE_VERSION, curl the page). Delete the test growth_events and shared_rooms rows you created only if Joe asks; otherwise leave them and list their ids.

F. Docs and wiki. Write docs/v2/phase-summaries/phase-growth-g0-g1-summary.md (what shipped across S1 to S4, the matrix results with evidence, defects and their status, what the plan got wrong across the phase, measures now live and their first real numbers if any). Update videx-wiki: growth-loops.md status to "G0/G1 verified on device" or the honest partial state, inbound-deep-linking.md and notifications-v1.md with anything device testing corrected, event-taxonomy.md verified notes, registers (parking-lot rows, next-steps: the growth block), log.md. Draft the strategy doc bump (D15): a v0.2 copy of docs/strategy/Videx_Growth_Loops_Strategy_v0.1.md with §3 corrected and §8 Q1/Q2 answered, as a separate commit for Joe to approve; do not snapshot it into raw/ (human-owned). End with a summary for the strategy thread: pass/fail table, defects and where they went, what Joe must still do (App Store submission of 2.5.0 is his call after the pass), and the G2 readiness view: what in the shipped stack the household loop can reuse and what it cannot.
```
