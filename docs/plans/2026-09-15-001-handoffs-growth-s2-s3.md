# Handoffs: Growth sessions S2 (attribution) and S3 (sign-in)

**Date:** 2026-09-15 · **Source:** [G0/G1 plan](2026-09-14-003-feat-phase-g0-g1-growth-foundations-and-sharing-plan.md) §4 (G0-5, G0-6), §6 (089, 090), §9, §13 · **Follows:** S1 links, merged 15 Sept (PRs #172, #183, #184; summary in the plan §11a). The two sessions run **in parallel** in separate worktrees; both prompts below carry a file-ownership list so they merge cleanly. S4 (sharing) starts after both merge.

**Shared rules for both sessions.** Worktree branch off `main`, relative paths, `native/src/lib` is a junction to `src/lib` (never recursive-delete inside `native/`), root `npm run lint` 0 errors, `npm test`, `npm run build`; native `npm run lint`, `npx tsc --noEmit`, `npx expo export --platform android`; wiki updated from the same worktree; PR footer standard. Do not bump app versions: the native rebuild happens once, after both sessions merge (Joe runs `ios-release.yml` and `android-release.yml`), and device checks are S5. Parking-lot ids are pre-assigned so the two sessions cannot collide: **S2 files `IN-GR-005` to `IN-GR-009`, S3 files `IN-GR-010` to `IN-GR-014`.** Both append to `videx-wiki/log.md`; whichever merges second resolves the append conflict on rebase (keep both entries, S2's first). `native/src/app/_layout.tsx` is the one file both touch: S2 adds one import and one `useEffect` inside `RootLayout`; S3 adds one `<Stack.Screen>` line. Keep to those hunks.

**Joe, before S2:** approve the privacy-policy paragraph the session drafts; the App Store and Play data forms are updated after merge (§9c).
**Joe, before S3:** hosted Supabase Auth provider state (the session checks it first and tells you what to enable); Google OAuth clients (web, iOS, Android with both SHA-1s) in the Firebase project `videx-3063b`; the iOS client id and web client id as EAS env vars and in `native/.env`.

---

## S2. Attribution and growth telemetry (G0-6, migration 090)

```
Context: Videx. Shared lib in src/lib (native/src/lib is a junction to it; never recursive-delete inside native/), live RN/Expo 56 app in native/ (expo-router, file routes under native/src/app), Cloudflare Worker in workers/api (Hono; pure modules unit-tested by the root vitest), Supabase migrations in supabase/migrations (apply is Joe's action in Studio, never db push). Read first, in this order: docs/plans/2026-09-14-003-feat-phase-g0-g1-growth-foundations-and-sharing-plan.md (§4 G0-6, §6 row 090, §9 D9/D10/D13/D17/D18, §11a S1 outcome), docs/CONVENTIONS.md, videx-wiki/wiki/concepts/decisions/adr-015-object-urls-and-inbound-links.md, videx-wiki/wiki/entities/codebase/event-taxonomy.md, videx-wiki/AGENTS.md. Load the cloudflare and workers-best-practices skills before touching the Worker. Worktree branch off main named feat/growth-s2-attribution, relative paths.

Runs in parallel with S3 (sign-in). You OWN: src/lib/growth/* (new files only; do not change parseInboundLink's contract), native/src/installId.ts, native/src/attribution.ts, native/src/app/+native-intent.tsx, native/src/app/curating.tsx, native/src/app/_layout.tsx (one import + one useEffect in RootLayout only), workers/api/**, supabase/migrations/090_*, supabase/queries/**, docs/legal/**. You must NOT touch native/src/providers/auth.tsx, native/src/components/auth/**, native/src/components/onboarding/**, native/app.json plugins, or any migration other than 090 (S3 owns those). Parking-lot ids IN-GR-005 to IN-GR-009 are yours.

Goal: every shared URL's ?via= and ?src= are recorded from the first page fetch to the install and the sign-up, through Videx's own pipeline (no third-party SDK), so the strategy's measures (shares per WAU, preview open rate, open-to-install, signup by source) can be computed from one table. Decided: a new growth_events table written only by the Worker; via = URL channel, src = originating session; self-minted install id; in-app pending link plus Android Play Install Referrer; no probabilistic iOS matching.

1. Migration supabase/migrations/090_growth_events.sql exactly as plan §6 row 090: growth_events (id bigint identity, occurred_at timestamptz not null default now(), event_name text check in ('preview_fetched','preview_opened','link_opened','first_open','signup_completed','share_initiated','share_completed','notification_opened'), install_id uuid, user_id uuid null references profiles(id) on delete cascade, via text, src text, object_type text, object_id text, platform text, ua_class text, delivery_id uuid null, metadata jsonb not null default '{}'); RLS on, REVOKE ALL from anon and authenticated (service-role writes only, same posture as shared_rooms in 088); indexes (user_id, occurred_at desc), (install_id), (event_name, occurred_at desc); a pg_cron job registered in this migration deleting rows older than 12 months nightly (follow the 079 rollup job's shape); delete_own_account() and export_user_data() rebuilt FROM THE 088 VERSIONS (read 088 first; they were rewritten there and in 086) with growth_events added: delete rows where user_id = the caller, export them under a "growth_events" key. Reversibility note in the header. After Joe applies it, regenerate src/lib/database.types.ts.

2. Install id and first touch (native). native/src/installId.ts: a UUID v4 minted once and kept in the existing MMKV 'videx' instance under 'install_id' (export the generator from src/lib/instrumentation/sessionId.ts or add src/lib/growth/uuid.ts; do not use expo-application, no permissions). native/src/attribution.ts: first-touch record { via, src, objectType, objectId, seenAt } written once and never overwritten (readFirstTouch, recordFirstTouch); cleared only with the install (not on sign-out). Sources of first touch, in priority order: (a) an inbound link, recorded in +native-intent.tsx right after S1's writePendingLink (reuse normaliseVia/normaliseSrc from src/lib/growth/inboundLink.ts); (b) on Android, the Play Install Referrer read once on first launch.

3. Play Install Referrer (D18). Evaluate react-native-play-install-referrer against Expo 56 with the new architecture (this app has no newArchEnabled:false, so the new architecture is on) and a clean expo prebuild in CI; if it passes, use it; if not, write a small local Expo module under native/modules/play-install-referrer (Expo Modules API, Kotlin, com.android.installreferrer:installreferrer:2.2, one async function getReferrer(): string | null) and say which you chose and why. Parse the referrer string with the same rules as a URL query. Also extend the Worker side so the referrer carries the object: in workers/api/src/pageShell.ts applyAttribution, the Play link's referrer becomes via=…&src=…&t=movie-603 (title) or &r={roomId} (room), URL-encoded; on Android first launch, a referrer with t= or r= writes a pending link (S1's writePendingLink with a synthetic InboundLink) so the app lands on the object after sign-up. This is the deterministic Android deferred deep link; iOS has none and that is accepted.

4. Client emitter src/lib/growth/growthEvents.ts (pure + one fetch): a typed GrowthEvent union per event_name with the fields above; postGrowthEvent(event) does a fire-and-forget POST to `${env.API_PROXY_URL}/v1/growth/events` with the install id, platform, and the Supabase access token as Bearer when a session exists (the Worker attaches user_id from the verified token; never send user_id from the client); no retry queue, drop on failure, never throws, never blocks render; unit-test the payload builder. Emit: first_open once per install id from native/src/app/_layout.tsx (a useEffect in RootLayout guarded by an MMKV 'first_open_sent' flag; include first touch if present); link_opened from +native-intent.tsx whenever parseInboundLink returns an object (via, src, object); signup_completed from native/src/app/curating.tsx next to the existing FIRST_HOME_VIEW log, carrying the first-touch via/src/object, and add via and src to that FIRST_HOME_VIEW onboarding_events metadata (extend OnboardingEventMetadata in src/lib/analytics/events.ts) so the existing funnel queries can split by source. Do not emit share_* or notification_opened (S4); they stay in the CHECK for S4 to use.

5. Worker ingest. POST /v1/growth/events: body validated in a pure workers/api/src/growthEvents.ts (event_name in the list, install_id uuid, object_type/object_id shapes from ADR-015, via/src through normaliseVia/normaliseSrc, metadata ≤ 2 KB, unknown keys rejected); optional Bearer Supabase JWT via the existing verifySupabaseJwt sets user_id; a second rate-limit binding GROWTH_RATELIMIT in wrangler.toml (namespace_id 1002, 60 per 60s) keyed on install_id, falling back to cf-connecting-ip; service-role insert; 204 on success, 400 on validation failure, 429 with Retry-After. CORS: same allow-list as today (the native app sends no Origin). Server-side page events: in the /t/:type/:ref and /room/:id handlers, after withAttribution, c.executionCtx.waitUntil an insert of preview_fetched (ua_class = crawler) or preview_opened (ua_class = human) with via/src/object; a pure workers/api/src/uaClass.ts classifies the User-Agent with a fixture of WhatsApp, facebookexternalhit, Slackbot, Twitterbot, Applebot, Googlebot, Discordbot, TelegramBot, LinkedInBot and a headless/empty UA (test it); confirmed in S1 that the Worker runs on cache hits, so this fires on every fetch. Never let the insert affect the response.

6. Measures. New supabase/queries/growth-dashboard.sql: shares per weekly active user (from user_interactions.share until S4 adds share_initiated; note the switch); preview_fetched vs preview_opened by object and via; link_opened → first_open → signup_completed funnel by via and platform; D7/D30 retention by first-touch via (join profiles.created_at); a note that Android open-to-install is deterministic via the referrer and iOS is inferred. Add a growth block to supabase/queries/metrics-dashboard.sql referencing it.

7. Data protection (D17). Draft one paragraph for docs/legal/privacy-policy.md (an anonymous install identifier and the link that brought you to Videx, used to measure sharing, kept 12 months, deleted with your account) and the matching rows in docs/legal/store-privacy-disclosures.md (Apple: Identifiers → Device ID? No: this is an app-generated id, declare under "Other Data"/"Product Interaction, analytics, not linked" and say why; Play: App activity → in-app actions, analytics). Joe approves the wording and files the forms; do not claim they are filed.

8. Verify: unit tests for growthEvents (client and Worker), uaClass, attribution, installId, the referrer parser and the referrer builder; root and native gates; workers/api npm run check; after Joe applies 090 and the Worker deploys, curl POST /v1/growth/events with a valid and an invalid body, fetch /t/movie/550-fight-club-1999?via=share with a WhatsApp UA and a Safari UA and show the two growth_events rows (Supabase MCP read-only select). Device checks are S5.

9. Docs and wiki: videx-wiki/wiki/entities/codebase/event-taxonomy.md (new growth_events section: destinations, fields, via/src, ua_class), entities/codebase/migrations.md (090), concepts/forward-planning/growth-loops.md status line, log.md; workers/api/README.md route list; file follow-ups as IN-GR-005..009. End with a summary for the strategy thread: what shipped, the curl and select outputs, what Joe must still do (apply 090, policy wording, store forms), what the plan got wrong.
```

## S3. Apple and Google sign-in (G0-5, migration 089)

```
Context: Videx. Live RN/Expo 56 app in native/ (expo-router, file routes under native/src/app; native/src/lib is a junction to the shared src/lib, never recursive-delete inside native/), Supabase Auth (email + password today), migrations in supabase/migrations (apply is Joe's action in Studio, never db push). Read first, in this order: docs/plans/2026-09-14-003-feat-phase-g0-g1-growth-foundations-and-sharing-plan.md (§2 item 4, §4 G0-5, §6 row 089, §9 D7/D8, §9b Q4, §9c S3 rows, §11a S1 outcome), docs/CONVENTIONS.md, native/src/providers/auth.tsx, native/src/components/auth/AuthScreen.tsx, native/src/components/onboarding/StepAccount.tsx and OnboardingFlow.tsx, native/src/app/auth.tsx and curating.tsx (S1's pending-link consumers: keep both paths), supabase/migrations/011_profiles_baseline.sql (handle_new_user), videx-wiki/AGENTS.md. Use the context7 tools for expo-apple-authentication, @react-native-google-signin/google-signin and Supabase signInWithIdToken before writing provider code. Worktree branch off main named feat/growth-s3-signin, relative paths.

Runs in parallel with S2 (attribution). You OWN: native/src/providers/auth.tsx, native/src/components/auth/**, native/src/components/onboarding/**, native/src/app/choose-username.tsx (new), native/src/app/(tabs)/_layout.tsx (only if the username gate needs it), native/app.json (plugins, ios.usesAppleSignIn), native/package.json, native/.env.example, supabase/migrations/089_*, docs/legal/store-privacy-disclosures.md (sign-in rows only). You must NOT touch native/src/app/+native-intent.tsx, curating.tsx, workers/api/**, src/lib/growth/**, any migration other than 089, and in native/src/app/_layout.tsx add only the one <Stack.Screen name="choose-username"> line (S2 owns the rest). Do not rewrite delete_own_account or export_user_data in 089 (S2 rewrites them in 090; a second rewrite would conflict). Parking-lot ids IN-GR-010 to IN-GR-014 are yours.

Goal: a person who arrives from a shared link can create an account in two taps. Decided: Apple and Google via supabase.auth.signInWithIdToken (native SDKs, not the browser flow); the Apple button on iOS only, Google on both platforms; auto-link on a matching verified email; a placeholder username from the trigger, claimed through a "Choose your name" prompt that is never shown as the raw placeholder; a provider sign-up skips the account step and continues onboarding from step 2.

1. First, report the hosted Supabase Auth state: which providers are enabled (Supabase MCP or dashboard; supabase/config.toml is local-only and says Apple off, no Google). Tell Joe exactly what to enable: Apple provider with app.videx.streaming in the authorised client ids (native id-token flow needs no Services ID or secret); Google provider with the web client id as Client ID and the iOS client id in authorised clients. Do not proceed to device-affecting code until you have written this list into the PR description.

2. Migration supabase/migrations/089_handle_new_user_placeholder.sql: handle_new_user() inserts coalesce(raw_user_meta_data->>'username', 'user_' || left(replace(new.id::text, '-', ''), 8)) and sets a new profiles.username_chosen boolean not null default true to false when the placeholder was used; keep username UNIQUE NOT NULL and the SECURITY DEFINER + search_path pin from 027; extend the username_available RPC (038) if it needs to exclude nothing new (it should not). Reversibility note. After Joe applies it, regenerate src/lib/database.types.ts (S2 also regenerates; whichever merges second re-runs typegen).

3. Dependencies and config. npx expo install expo-apple-authentication @react-native-google-signin/google-signin expo-crypto (check the native lockfile stays in sync; the July staleness was repaired, so a clean diff is expected). native/app.json: add the expo-apple-authentication plugin and ios.usesAppleSignIn: true; add the @react-native-google-signin/google-signin plugin with iosUrlScheme set from the reversed iOS client id via an env-driven app.config.js value (do not commit client ids; read EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID and EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID from native/.env and EAS env; document both in native/.env.example and the release runbook). Android uses the committed google-services.json; state which SHA-1s Joe must register in Firebase (upload key and Play App Signing key are the same key per S1, plus the debug keystore if a dev build should work).

4. Provider code in native/src/providers/auth.tsx: signInWithApple() (generate a raw nonce, hash it with expo-crypto SHA-256, pass the hash to AppleAuthentication.signInAsync with FULL_NAME and EMAIL scopes, then supabase.auth.signInWithIdToken({ provider: 'apple', token: identityToken, nonce: rawNonce })); signInWithGoogle() (GoogleSignin.configure({ webClientId, iosClientId }) once, hasPlayServices, signIn, then signInWithIdToken({ provider: 'google', token: idToken })). Both return the same shape as signInWithPassword and clear nothing that the pending link needs. Errors: user-cancelled is silent; anything else surfaces the existing error copy. Expose isAppleAvailable (iOS only and AppleAuthentication.isAvailableAsync()).

5. UI. native/src/components/auth/AuthScreen.tsx: provider buttons above the email form (Apple's button via AppleAuthenticationButton in the black style on iOS; a Google button matching the app's button primitives and Google's branding rules), then an "or" divider, then the existing fields. StepAccount.tsx: the same two buttons above the form with "Continue with"; on success call onAccountCreated so the flow advances to Connect Services. OnboardingFlow.tsx: if a session already exists when the flow mounts (a provider sign-in from /auth that has no onboarding yet, redirected by the tabs guard), start at step 2 and skip the account step. Keep the pending-link behaviour S1 added in auth.tsx: after a provider sign-in on /auth the focus effect replaces to / and pushes the pending route; after a provider sign-up the link resumes through curating.tsx. Do not change either file.

6. Choose your name. New native/src/app/choose-username.tsx (modal presentation, registered with one Stack.Screen line in _layout.tsx): shown once when profiles.username_chosen is false, prefilled from the provider's given name when Apple or Google supplied one (Apple only sends it on the first sign-in; capture it in step 4 and keep it in memory for this prompt only), validated with the existing username_available RPC, saved with updateUser({ data: { username } }) plus a profiles update setting username_chosen = true (mirror ProfileAccount.tsx). Gate: wherever the app already reads the profile after sign-in (the onboarding status query or the profile hook), add username_chosen and redirect to the prompt when false, after onboarding is complete and before the tabs render; the raw placeholder must never appear in the profile screen or the header. Existing users are unaffected (default true).

7. Account deletion and Apple. App Store guideline 5.1.1(v) requires apps that offer Sign in with Apple to revoke the Apple token when the user deletes the account; Supabase does not do this for you. Do NOT build it in S3: file it as IN-GR-010 with the shape (an Edge Function called from delete_own_account's client path that posts to appleid.apple.com/auth/revoke with a client secret signed by an Apple key; needs a Services ID and a .p8 key from Joe) and flag it to Joe as a decision, since it is a review risk once Apple sign-in ships.

8. Privacy labels: docs/legal/store-privacy-disclosures.md gains Name and Email Address under Contact Info for account creation (both stores); note Apple's private relay emails. Joe files the forms.

9. Verify: unit-test the nonce helper and the placeholder-username derivation (a SQL fixture is fine); root and native gates; npx expo export --platform android; confirm npx expo prebuild --platform ios --no-install in a temp dir shows the Sign in with Apple entitlement and the Google URL scheme in Info.plist (read the generated files, then delete the temp dir; never prebuild inside native/). Apple sign-in cannot run in a simulator or on Android; device checks are S5 and need the rebuild.

10. Docs and wiki: docs/v2/launch/release-runbook.md (new env vars, the entitlement, the Firebase SHA-1 step, the Supabase provider settings); videx-wiki/wiki/concepts/product/privacy-and-gdpr.md (sign-in providers), entities/codebase/migrations.md (089), concepts/forward-planning/growth-loops.md status line, log.md; IN-GR-010..014 for follow-ups. End with a summary for the strategy thread: what shipped, the exact Supabase and Google console steps still open for Joe, the entitlement evidence from prebuild, what the plan got wrong.
```
