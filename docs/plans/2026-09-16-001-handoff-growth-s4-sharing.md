# Handoff: Growth session S4, sharing (G1-1 to G1-4)

**Date:** 2026-09-16 · **Source:** [G0/G1 plan](2026-09-14-003-feat-phase-g0-g1-growth-foundations-and-sharing-plan.md) §5 (G1-1 to G1-4), §9 D13/D14, §9b, §11a, §11b · **Follows:** S1 links (merged 15 Sept), S2 attribution (merged 16 Sept, PR #188) and S3 sign-in (merged 16 Sept, PRs #187 and #190). S4 is the last build session before the rebuild; S5 verification follows it.

**Joe, before starting:** nothing. During the session you deploy `send-notifications` once the Edge Function change is reviewed (`npx supabase functions deploy send-notifications`, keep `verify_jwt = true`).

---

```
Context: Videx. Shared lib in src/lib (native/src/lib is a junction to it; never recursive-delete inside native/), live RN/Expo 56 app in native/ (expo-router, file routes under native/src/app), Supabase Edge Function supabase/functions/send-notifications (Deno; deploy is Joe's action), growth telemetry from S2 (src/lib/growth/growthEvents.ts, native/src/attribution.ts, table growth_events, migration 090 applied). Read first, in this order: docs/plans/2026-09-14-003-feat-phase-g0-g1-growth-foundations-and-sharing-plan.md (§5 G1-1 to G1-4, §9 D13/D14, §9b product answers, §11a and §11b session outcomes), docs/CONVENTIONS.md, native/src/components/ShareButton.tsx, native/src/app/detail/[id].tsx and room/[id].tsx, native/src/providers/notifications.tsx, supabase/functions/send-notifications/index.ts (composeMessage and the claim step), src/lib/growth/growthEvents.ts (the GrowthEvent union already defines share_initiated, share_completed and notification_opened), native/src/attribution.ts (sendGrowthEvent), supabase/queries/growth-dashboard.sql §1, videx-wiki/wiki/entities/codebase/event-taxonomy.md, videx-wiki/wiki/concepts/architecture/notifications-v1.md, videx-wiki/wiki/concepts/product/tone-and-voice-guide.md, videx-wiki/AGENTS.md. Worktree branch off main named feat/growth-s4-sharing, relative paths.

Nothing else runs in parallel: main carries S1 to S3 (links, attribution, sign-in). Expected files: native/src/components/ShareButton.tsx, native/src/app/detail/[id].tsx, native/src/app/room/[id].tsx, the For You room-card share wiring (native/src/components/MoodRooms.tsx or wherever S1 put it), native/src/providers/notifications.tsx, new src/lib/growth/shareCopy.ts and src/lib/instrumentation/sessionOrigin.ts, additive edits to src/lib/growth/growthEvents.ts and native/src/attribution.ts, supabase/functions/send-notifications/**, supabase/queries/growth-dashboard.sql and metrics-dashboard.sql, docs/strategy/briefs/h0-device-test-checklist.md. Leave the S3 surfaces alone unless a change is unavoidable (native/src/providers/auth.tsx, native/src/components/auth/**, native/src/components/onboarding/**, native/src/app/confirm-email.tsx and choose-username.tsx); no migration is needed. Be aware of two S3 behaviours: auth.tsx resumes a pending link only for onboarded accounts (new accounts get it after Curating), and Confirm email is still off in hosted Auth by design. No Worker page markup changes are expected; if you must touch workers/api/src/pageShell.ts or titlePage.ts, bump PAGE_CACHE_VERSION. Parking-lot ids IN-GR-023 to IN-GR-027 are yours (015 to 019 were reserved earlier and are unused; S3 filed 020 to 022).

Goal: the share sheet carries the UK availability line (the hook), every share is measured from tap to completion with its source, and a push tap becomes a share moment: the notification deep-links to the title and the detail page offers "Tell someone" with copy for that moment. Decided (do not reopen): native share sheet, lucide Share2 glyph in the existing top-right position on detail and room screens, no feature flag; via = URL channel and src = originating session (push | organic); "Tell someone" on arrival (the stronger nudge) and leaving-soon single-title pushes, nothing on bundles; notification opens are growth_events rows with delivery_id; the existing user_interactions.share event stays as it is.

1. Share copy (src/lib/growth/shareCopy.ts, pure, tests). buildTitleShareCopy({ title, year, subscriptionServices, rentBuyServices, url }) -> { message, url }: "Severance (2022). On Apple TV+ in the UK.\n{url}" when streaming on a subscription or free service; "Rent or buy on Apple TV and Prime in the UK." when rent or buy only; "See where to watch in the UK on Videx." when nothing in the UK (still shareable). Service labels must match the Worker page (SHARE_SERVICE_LABELS in workers/api/src/titlePage.ts; move that map into src/lib/growth/serviceLabels.ts and import it from both places so they cannot drift). Addon rows excluded, per migration 084. Two or more services join with "and"; four or more become "Netflix, NOW and 2 more". buildRoomShareCopy({ label, count, url }) -> "{label}: {count} titles picked for the mood.\n{url}". Tone guide: no exclamation marks, never the word AI. The URL carries ?via=share and, when the session origin is push, &src=push (append with a small helper; the canonical URL from titlePageUrl / sharedRoomUrl is unchanged).

2. ShareButton (native/src/components/ShareButton.tsx). Title variant gains an availability prop { subscriptionServices: string[]; rentBuyServices: string[] } that the detail page derives from its detail data (useContentDetail → the adapter's stream options; exclude addon/channel options; if the adapter exposes channelOptions separately, ignore them), plus an optional moment prop for the "Tell someone" state (step 5). URL variant (rooms) gains label and count for the room copy. On tap: sendGrowthEvent({ name: 'share_initiated', object, via: 'share', src: current session origin, metadata: { surface: 'detail' | 'room' | 'room_card', moment? } }); on Share.sharedAction: share_completed with the same object and metadata plus to_surface (activityType) and platform_reports_completion: Platform.OS === 'ios' (Android reports sharedAction on dismiss, so the dashboard treats Android completion as unreliable). Keep emitShare (user_interactions.share) exactly as it is for titles; rooms log nothing there. The room-card share on For You passes label and count too.

3. Notification payload (supabase/functions/send-notifications/index.ts). ExpoMessage.data becomes { url, type, delivery_id: string | null, via: 'push', service_id?: string, expires_on?: string }. For a single-title push, delivery_id is the id of that title's claimed notification_deliveries row (the claim upsert returns ids; thread the row through composeMessage) and service_id / expires_on come from the candidate; for bundles delivery_id is null and the extra fields are omitted. Unit-test composeMessage for both shapes (the function has a test pattern; if not, add a small Deno test and run deno test alongside deno check). Do not change dedup, cap or send logic. Joe deploys it; say so in the PR.

4. Notification open (native/src/providers/notifications.tsx). routeFromData now also: parses data.url with parseInboundLink (src/lib/growth/inboundLink.ts) to get the object; calls sendGrowthEvent({ name: 'notification_opened', object, deliveryId: data.delivery_id ?? null, via: 'push', src: 'push', metadata: { type: data.type } }); sets the session origin (step 5) to { origin: 'push', object, type: data.type, serviceId: data.service_id, expiresOn: data.expires_on, at: Date.now() } before router.push. Cold start and warm paths both go through it. Keep the existing bypass of +native-intent.

5. Session origin (src/lib/instrumentation/sessionOrigin.ts, pure, tests). setSessionOrigin(record) / getSessionOrigin(): in-memory, cleared by the sessionId module's onSessionReset callback so a push-originated session ends when the app has been backgrounded for the session timeout; getSessionSrc(): 'push' | 'organic' for the share events and the URL. "Tell someone" on the detail page: when getSessionOrigin() is push and its object matches the page's title, render the share affordance in its prominent moment state (same top-right position; plus a one-line banner under the hero title, dismissible, using the app's existing banner or pill primitives): arrival copy "{Title} has just landed on {Service}. Tell someone." and leaving-soon copy "{Title} leaves {Service} on {formatted expires_on}. Tell someone." Tapping either opens the same share sheet with the title copy from step 1 prefixed by the moment line ("Just landed on Apple TV+: Severance (2022). …"). Service label from the shared serviceLabels map. No state for bundles (they land on the watchlist).

6. Measures (supabase/queries/growth-dashboard.sql). Switch §1 to share_initiated per the comment left by S2 (keep the old CTE commented for the transition); add: share completion rate iOS only (share_completed / share_initiated where platform = 'ios'); share rate for push-originated sessions vs organic (share_initiated by src, divided by notification_opened for push and by active users for organic); notification CTR (notification_opened joined to notification_deliveries on delivery_id, by notification_type); "Tell someone" take-up (share_initiated with metadata.moment). Reference the new blocks from metrics-dashboard.sql's growth section. Run each read-only against production (Supabase MCP) and paste the shapes into the PR; rows will be empty until devices exist, and that is fine.

7. Device checklist (docs/strategy/briefs/h0-device-test-checklist.md): add the S4 items for S5: share a title with a subscription service, a rent-or-buy-only title, and an unavailable title, checking the copy in WhatsApp and Messages; share a room from a card and from the room screen; run send-notifications for the test account, tap the push, confirm the notification_opened row carries delivery_id and the "Tell someone" banner shows with the right copy, share from it and confirm src=push in the URL and on the share events; background the app for six minutes, return, confirm the banner is gone.

8. Verify: root npm run lint (0 errors), npm test, npm run build; native npm run lint, npx tsc --noEmit, npx expo export --platform android; deno check (and deno test if added) for the Edge Function. No Worker change means no deploy; if you did touch a page, curl it after deploy and confirm the new PAGE_CACHE_VERSION serves. Nothing here runs on a device before S5.

9. Docs and wiki: videx-wiki/wiki/entities/codebase/event-taxonomy.md (share_initiated, share_completed, notification_opened rows with their metadata; note user_interactions.share is kept for the ranking-side history), wiki/concepts/architecture/notifications-v1.md (payload fields, opened event, the "Tell someone" moment), concepts/forward-planning/growth-loops.md status line, log.md; IN-GR-023..027 for follow-ups. End with a summary for the strategy thread: what shipped, the dashboard query shapes, what Joe must do (deploy the function; nothing else), what the plan got wrong.
```
